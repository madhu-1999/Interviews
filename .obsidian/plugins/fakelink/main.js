/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
if you want to view the source, please visit the github repository of this plugin
*/

var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// main.ts
var main_exports = {};
__export(main_exports, {
  default: () => LinkerPlugin
});
module.exports = __toCommonJS(main_exports);
var import_obsidian6 = require("obsidian");
var import_view2 = require("@codemirror/view");
var import_state2 = require("@codemirror/state");

// src/lang/helpers.ts
var _a, _b;
var lang = ((_b = (_a = window.moment) == null ? void 0 : _a.locale) == null ? void 0 : _b.call(_a)) || "en";
var zhCN = {
  "Matching behavior": "\u5339\u914D\u884C\u4E3A",
  "Case sensitivity": "\u5927\u5C0F\u5199",
  "Matched files": "\u5339\u914D\u6587\u4EF6",
  "Link style": "\u94FE\u63A5\u6837\u5F0F",
  "Auto-toggle activation status by mode": "\u6309\u6A21\u5F0F\u81EA\u52A8\u5207\u6362\u6FC0\u6D3B\u72B6\u6001",
  "When enabled, the plugin will automatically activate in edit mode if inactive, and automatically deactivate in read mode if active": "\u542F\u7528\u540E\uFF0C\u63D2\u4EF6\u5C06\u5728\u7F16\u8F91\u6A21\u5F0F\u4E2D\u81EA\u52A8\u6FC0\u6D3B\uFF0C\u5728\u9605\u8BFB\u6A21\u5F0F\u4E2D\u81EA\u52A8\u505C\u7528",
  "Activate virtual linker": "\u6FC0\u6D3B\u865A\u62DF\u94FE\u63A5",
  "Due to table and canvas rendering issues, to fully enable/disable virtual link rendering, use Quick Add or a third-party plugin to toggle the Fake Link plugin on/off.": "\u7531\u4E8E\u8868\u683C\u548C Canvas \u6E32\u67D3\u95EE\u9898\uFF0C\u5982\u9700\u5B8C\u5168\u5F00\u5173\u865A\u62DF\u94FE\u63A5\u6E32\u67D3\uFF0C\u8BF7\u4F7F\u7528 Quick Add \u6216\u7B2C\u4E09\u65B9\u63D2\u4EF6\u6765\u5B8C\u5168\u5F00\u542F/\u5173\u95ED Fake Link \u63D2\u4EF6\u3002",
  "Copy Quick Add script": "\u590D\u5236 Quick Add \u811A\u672C",
  "Quick Add script copied to clipboard!": "Quick Add \u811A\u672C\u5DF2\u590D\u5236\u5230\u526A\u8D34\u677F\uFF01",
  "Copy EasyTyping template": "\u590D\u5236 EasyTyping \u6A21\u677F",
  "EasyTyping template copied to clipboard!": "EasyTyping \u6A21\u677F\u5DF2\u590D\u5236\u5230\u526A\u8D34\u677F\uFF01",
  "Show advanced settings": "\u663E\u793A\u9AD8\u7EA7\u8BBE\u7F6E",
  "Include aliases": "\u5305\u542B\u522B\u540D",
  "If enabled, the virtual linker will also match file aliases.": "\u542F\u7528\u540E\uFF0C\u865A\u62DF\u94FE\u63A5\u4E5F\u4F1A\u5339\u914D\u6587\u4EF6\u7684\u522B\u540D\u3002",
  "Only link once": "\u4EC5\u94FE\u63A5\u4E00\u6B21",
  "When enabled, identical terms in the same note will only be linked once.": "\u542F\u7528\u540E\uFF0C\u540C\u4E00\u7B14\u8BB0\u4E2D\u76F8\u540C\u7684\u8BCD\u53EA\u4F1A\u88AB\u94FE\u63A5\u4E00\u6B21\u3002",
  "Exclude links to real linked files": "\u6392\u9664\u5DF2\u624B\u52A8\u94FE\u63A5\u7684\u6587\u4EF6",
  "When enabled, terms that are already manually linked in the note will not be auto-linked.": "\u542F\u7528\u540E\uFF0C\u7B14\u8BB0\u4E2D\u5DF2\u5B58\u5728\u7684\u624B\u52A8\u94FE\u63A5\u4E0D\u4F1A\u88AB\u81EA\u52A8\u94FE\u63A5\u3002",
  "Include headers": "\u5305\u542B\u6807\u9898",
  "When enabled, Markdown headings (lines starting with #) will also be included for virtual linking.": "\u542F\u7528\u540E\uFF0CMarkdown \u6807\u9898\uFF08\u4EE5 # \u5F00\u5934\u7684\u884C\uFF09\u4E5F\u4F1A\u88AB\u5305\u542B\u5728\u865A\u62DF\u94FE\u63A5\u4E2D\u3002",
  "Enable header symbol keywords": "\u542F\u7528\u6807\u9898\u7B26\u53F7\u5173\u952E\u8BCD",
  "When enabled, text between start and end symbols in headers will be used as virtual link keywords. Tip: use EasyTyping to select text and add symbols.": "\u542F\u7528\u540E\uFF0C\u6807\u9898\u4E2D\u8D77\u6B62\u7B26\u53F7\u4E4B\u95F4\u7684\u6587\u672C\u5C06\u88AB\u7528\u4F5C\u865A\u62DF\u94FE\u63A5\u5173\u952E\u8BCD\u3002\u63A8\u8350\u4F7F\u7528 EasyTyping \u7B49\u5DE5\u5177\u9009\u4E2D\u6587\u5B57\u5E76\u6DFB\u52A0\u5BF9\u5E94\u7B26\u53F7\u3002",
  "Start symbol": "\u8D77\u59CB\u7B26\u53F7",
  "Symbol marking the start of the keyword in headers. Must be different from end symbol.": "\u6807\u8BB0\u6807\u9898\u4E2D\u5173\u952E\u8BCD\u8D77\u59CB\u4F4D\u7F6E\u7684\u7B26\u53F7\u3002\u5FC5\u987B\u4E0E\u7ED3\u675F\u7B26\u53F7\u4E0D\u540C\u3002",
  "End symbol": "\u7ED3\u675F\u7B26\u53F7",
  "Symbol marking the end of the keyword in headers. Must be different from start symbol.": "\u6807\u8BB0\u6807\u9898\u4E2D\u5173\u952E\u8BCD\u7ED3\u675F\u4F4D\u7F6E\u7684\u7B26\u53F7\u3002\u5FC5\u987B\u4E0E\u8D77\u59CB\u7B26\u53F7\u4E0D\u540C\u3002",
  "Only match headers between symbols": "\u4EC5\u5339\u914D\u542B\u7B26\u53F7\u7684\u6807\u9898",
  "When enabled, only headers containing start and end symbols will produce virtual links. Unmarked headers will not produce virtual links.": "\u542F\u7528\u540E\uFF0C\u53EA\u6709\u5305\u542B\u8D77\u6B62\u7B26\u53F7\u7684\u6807\u9898\u624D\u4F1A\u4EA7\u751F\u865A\u62DF\u94FE\u63A5\u3002\u65E0\u6807\u8BB0\u7684\u6807\u9898\u4E0D\u4F1A\u4EA7\u751F\u865A\u62DF\u94FE\u63A5\u3002",
  "Allow virtual links in headers": "\u5141\u8BB8\u6807\u9898\u4E2D\u7684\u865A\u62DF\u94FE\u63A5",
  "When enabled, virtual links will be displayed inside Markdown headings. Tip: use with Quick Switcher++ for header navigation.": "\u542F\u7528\u540E\uFF0C\u865A\u62DF\u94FE\u63A5\u5C06\u663E\u793A\u5728 Markdown \u6807\u9898\u4E2D\u3002\u53EF\u4EE5\u914D\u5408 Quick Switcher++ \u8FDB\u884C\u6807\u9898\u7684\u68C0\u7D22\u4F7F\u7528\u3002",
  "Match any part of a word": "\u5339\u914D\u5355\u8BCD\u7684\u4EFB\u610F\u90E8\u5206",
  "When disabled, only complete word matches are linked. When enabled, any substring match will be linked.": "\u5173\u95ED\u65F6\u4EC5\u5339\u914D\u5B8C\u6574\u5355\u8BCD\u3002\u542F\u7528\u65F6\u4EFB\u4F55\u5B50\u4E32\u5339\u914D\u90FD\u4F1A\u4EA7\u751F\u94FE\u63A5\u3002",
  "Match the beginning of words": "\u5339\u914D\u5355\u8BCD\u5F00\u5934",
  "When enabled, word prefixes will be linked even without complete word matches.": "\u542F\u7528\u540E\uFF0C\u5355\u8BCD\u524D\u7F00\u5373\u4F7F\u4E0D\u662F\u5B8C\u6574\u5339\u914D\u4E5F\u4F1A\u88AB\u94FE\u63A5\u3002",
  "Match the end of words": "\u5339\u914D\u5355\u8BCD\u7ED3\u5C3E",
  "When enabled, word suffixes will be linked even without complete word matches.": "\u542F\u7528\u540E\uFF0C\u5355\u8BCD\u540E\u7F00\u5373\u4F7F\u4E0D\u662F\u5B8C\u6574\u5339\u914D\u4E5F\u4F1A\u88AB\u94FE\u63A5\u3002",
  "Suppress suffix for sub words": "\u90E8\u5206\u5339\u914D\u65F6\u4E0D\u663E\u793A\u540E\u7F00",
  "When enabled, the link suffix will only be shown for complete word matches, not partial matches.": "\u542F\u7528\u540E\uFF0C\u94FE\u63A5\u540E\u7F00\u4EC5\u5BF9\u5B8C\u6574\u5355\u8BCD\u5339\u914D\u663E\u793A\uFF0C\u90E8\u5206\u5339\u914D\u4E0D\u663E\u793A\u3002",
  "Fix ime typing issues": "\u4FEE\u590D\u8F93\u5165\u6CD5\u95EE\u9898",
  "This option is recommended when using ime for typing non-latin scripts such as chinese, japanese, or korean and prevents virtual linking from interfering with ime composition at the start of lines.": "\u4F7F\u7528\u8F93\u5165\u6CD5\u8F93\u5165\u4E2D\u6587\u3001\u65E5\u6587\u3001\u97E9\u6587\u7B49\u975E\u62C9\u4E01\u6587\u5B57\u65F6\u63A8\u8350\u5F00\u542F\uFF0C\u53EF\u9632\u6B62\u865A\u62DF\u94FE\u63A5\u5E72\u6270\u884C\u9996\u7684\u8F93\u5165\u6CD5\u7EC4\u5408\u3002",
  "Avoid linking in current line": "\u5F53\u524D\u884C\u4E0D\u663E\u793A\u94FE\u63A5",
  "If activated, there will be no links in the current line.": "\u542F\u7528\u540E\uFF0C\u5F53\u524D\u884C\u4E0D\u4F1A\u663E\u793A\u4EFB\u4F55\u865A\u62DF\u94FE\u63A5\u3002",
  "Maximum references to show": "\u6700\u591A\u663E\u793A\u5F15\u7528\u6570",
  "The maximum number of reference markers [1][2]... shown after a virtual link.": "\u865A\u62DF\u94FE\u63A5\u540E\u663E\u793A\u7684\u5F15\u7528\u6807\u8BB0 [1][2]... \u7684\u6700\u5927\u6570\u91CF\u3002",
  "Hide link when references exceed": "\u5F15\u7528\u8FC7\u591A\u65F6\u9690\u85CF\u94FE\u63A5",
  "When the total number of matching files (names + aliases + headers) exceeds this threshold, the virtual link will not be displayed.": "\u5F53\u5339\u914D\u7684\u6587\u4EF6\u603B\u6570\uFF08\u6587\u4EF6\u540D + \u522B\u540D + \u6807\u9898\uFF09\u8D85\u8FC7\u6B64\u9608\u503C\u65F6\uFF0C\u5C06\u4E0D\u663E\u793A\u8BE5\u865A\u62DF\u94FE\u63A5\u3002",
  "Case sensitive": "\u533A\u5206\u5927\u5C0F\u5199",
  "If activated, the matching is case sensitive.": "\u542F\u7528\u540E\uFF0C\u5339\u914D\u5C06\u533A\u5206\u5927\u5C0F\u5199\u3002",
  "Capital letter percentage for automatic match case": "\u81EA\u52A8\u533A\u5206\u5927\u5C0F\u5199\u7684\u5927\u5199\u6BD4\u4F8B",
  "The percentage (0 - 100) of capital letters in a file name or alias to be automatically considered as case sensitive.": "\u6587\u4EF6\u540D\u6216\u522B\u540D\u4E2D\u5927\u5199\u5B57\u6BCD\u7684\u767E\u5206\u6BD4\uFF080-100\uFF09\uFF0C\u8D85\u8FC7\u6B64\u9608\u503C\u5C06\u81EA\u52A8\u89C6\u4E3A\u533A\u5206\u5927\u5C0F\u5199\u3002",
  "Tag to ignore case": "\u5FFD\u7565\u5927\u5C0F\u5199\u7684\u6807\u7B7E",
  "By adding this tag to a file, the linker will ignore the case for the file.": "\u7ED9\u6587\u4EF6\u6DFB\u52A0\u6B64\u6807\u7B7E\u540E\uFF0C\u8BE5\u6587\u4EF6\u7684\u5339\u914D\u5C06\u4E0D\u533A\u5206\u5927\u5C0F\u5199\u3002",
  "Tag to match case": "\u533A\u5206\u5927\u5C0F\u5199\u7684\u6807\u7B7E",
  "By adding this tag to a file, the linker will match the case for the file.": "\u7ED9\u6587\u4EF6\u6DFB\u52A0\u6B64\u6807\u7B7E\u540E\uFF0C\u8BE5\u6587\u4EF6\u7684\u5339\u914D\u5C06\u533A\u5206\u5927\u5C0F\u5199\u3002",
  "Property name to ignore case": "\u5FFD\u7565\u5927\u5C0F\u5199\u7684\u5C5E\u6027\u540D",
  "By adding this property to a note, containing a list of names, the linker will ignore the case for the specified names / aliases.": "\u5728\u7B14\u8BB0\u4E2D\u6DFB\u52A0\u6B64\u5C5E\u6027\uFF08\u5305\u542B\u540D\u79F0\u5217\u8868\uFF09\uFF0C\u6307\u5B9A\u7684\u540D\u79F0/\u522B\u540D\u5C06\u4E0D\u533A\u5206\u5927\u5C0F\u5199\u3002",
  "By adding this property to a note, containing a list of names, the linker will ignore the case for the specified names / aliases. This way you can decide, which alias should be insensitive.": "\u5728\u7B14\u8BB0\u4E2D\u6DFB\u52A0\u6B64\u5C5E\u6027\uFF08\u5305\u542B\u540D\u79F0\u5217\u8868\uFF09\uFF0C\u6307\u5B9A\u7684\u540D\u79F0/\u522B\u540D\u5C06\u4E0D\u533A\u5206\u5927\u5C0F\u5199\u3002",
  "Property name to match case": "\u533A\u5206\u5927\u5C0F\u5199\u7684\u5C5E\u6027\u540D",
  "By adding this property to a note, containing a list of names, the linker will match the case for the specified names / aliases.": "\u5728\u7B14\u8BB0\u4E2D\u6DFB\u52A0\u6B64\u5C5E\u6027\uFF08\u5305\u542B\u540D\u79F0\u5217\u8868\uFF09\uFF0C\u6307\u5B9A\u7684\u540D\u79F0/\u522B\u540D\u5C06\u533A\u5206\u5927\u5C0F\u5199\u3002",
  "By adding this property to a note, containing a list of names, the linker will match the case for the specified names / aliases. This way you can decide, which alias should be case sensitive.": "\u5728\u7B14\u8BB0\u4E2D\u6DFB\u52A0\u6B64\u5C5E\u6027\uFF08\u5305\u542B\u540D\u79F0\u5217\u8868\uFF09\uFF0C\u6307\u5B9A\u7684\u540D\u79F0/\u522B\u540D\u5C06\u533A\u5206\u5927\u5C0F\u5199\u3002",
  "Include all files": "\u5305\u542B\u6240\u6709\u6587\u4EF6",
  "Include all files for the virtual linker.": "\u4E3A\u865A\u62DF\u94FE\u63A5\u5305\u542B\u6240\u6709\u6587\u4EF6\u3002",
  "Glossary linker directories": "\u8BCD\u5E93\u76EE\u5F55",
  "Directories to include for the virtual linker (separated by new lines).": "\u865A\u62DF\u94FE\u63A5\u8981\u5305\u542B\u7684\u76EE\u5F55\uFF08\u6BCF\u884C\u4E00\u4E2A\uFF09\u3002",
  "Excluded directories": "\u6392\u9664\u7684\u76EE\u5F55",
  "Directories from which files are to be excluded for the virtual linker (separated by new lines). Files in these directories will not create any virtual links in other files.": "\u865A\u62DF\u94FE\u63A5\u8981\u6392\u9664\u7684\u76EE\u5F55\uFF08\u6BCF\u884C\u4E00\u4E2A\uFF09\u3002\u8FD9\u4E9B\u76EE\u5F55\u4E2D\u7684\u6587\u4EF6\u4E0D\u4F1A\u5728\u5176\u4ED6\u6587\u4EF6\u4E2D\u521B\u5EFA\u865A\u62DF\u94FE\u63A5\u3002",
  "Tag to include file": "\u5305\u542B\u6587\u4EF6\u7684\u6807\u7B7E",
  "Tag to explicitly include the file for the linker.": "\u7ED9\u6587\u4EF6\u6DFB\u52A0\u6B64\u6807\u7B7E\u540E\uFF0C\u8BE5\u6587\u4EF6\u5C06\u88AB\u663E\u5F0F\u5305\u542B\u3002",
  "Tag to ignore file": "\u6392\u9664\u6587\u4EF6\u7684\u6807\u7B7E",
  "Tag to ignore the file for the linker.": "\u7ED9\u6587\u4EF6\u6DFB\u52A0\u6B64\u6807\u7B7E\u540E\uFF0C\u8BE5\u6587\u4EF6\u5C06\u88AB\u6392\u9664\u3002",
  "Exclude self-links to the current note": "\u6392\u9664\u6307\u5411\u81EA\u8EAB\u7684\u94FE\u63A5",
  "If toggled, links to the note itself are excluded from the linker.": "\u542F\u7528\u540E\uFF0C\u6307\u5411\u5F53\u524D\u7B14\u8BB0\u81EA\u8EAB\u7684\u94FE\u63A5\u5C06\u88AB\u6392\u9664\u3002",
  "If toggled, links to the note itself are excluded from the linker, but this might not work in reading view.": "\u542F\u7528\u540E\uFF0C\u6307\u5411\u5F53\u524D\u7B14\u8BB0\u81EA\u8EAB\u7684\u94FE\u63A5\u5C06\u88AB\u6392\u9664\u3002",
  "Excluded directories for generating virtual links": "\u4E0D\u751F\u6210\u94FE\u63A5\u7684\u76EE\u5F55",
  "Directories in which the plugin will not create virtual links (separated by new lines).": "\u8FD9\u4E9B\u76EE\u5F55\u4E2D\u4E0D\u4F1A\u751F\u6210\u865A\u62DF\u94FE\u63A5\uFF08\u6BCF\u884C\u4E00\u4E2A\uFF09\u3002",
  "Excluded keywords": "\u6392\u9664\u7684\u5173\u952E\u8BCD",
  "Keywords to exclude from virtual linking (comma separated). Files/aliases or headings matching these keywords will not be linked.": "\u8981\u6392\u9664\u7684\u5173\u952E\u8BCD\uFF08\u9017\u53F7\u5206\u9694\uFF09\u3002\u5339\u914D\u8FD9\u4E9B\u5173\u952E\u8BCD\u7684\u6587\u4EF6\u3001\u522B\u540D\u6216\u6807\u9898\u4E0D\u4F1A\u88AB\u94FE\u63A5\u3002",
  "Excluded file extensions": "\u6392\u9664\u7684\u6587\u4EF6\u6269\u5C55\u540D",
  "File extensions to exclude from virtual linking (one per line or comma separated)": "\u8981\u6392\u9664\u7684\u6587\u4EF6\u6269\u5C55\u540D\uFF08\u6BCF\u884C\u4E00\u4E2A\u6216\u9017\u53F7\u5206\u9694\uFF09",
  "Auto-insert symbol into headers": "\u81EA\u52A8\u63D2\u5165\u6807\u9898\u7B26\u53F7",
  "When enabled, a unique symbol is automatically placed at the front of new or modified header text, preventing accidental matching by regular body text.": "\u542F\u7528\u540E\uFF0C\u5C06\u5728\u65B0\u5EFA\u6216\u4FEE\u6539\u7684\u6807\u9898\u6587\u672C\u524D\u81EA\u52A8\u63D2\u5165\u4E00\u4E2A\u72EC\u7279\u7B26\u53F7\uFF0C\u9632\u6B62\u88AB\u6B63\u6587\u610F\u5916\u5339\u914D\u3002",
  "Header marker symbol": "\u6807\u9898\u6807\u8BB0\u7B26\u53F7",
  "The symbol placed at the front of header text (after # but before content). Use a rare character not found in normal text.": "\u653E\u7F6E\u5728\u6807\u9898\u6587\u672C\u524D\u7684\u7B26\u53F7\uFF08\u5728 # \u4E4B\u540E\u3001\u5185\u5BB9\u4E4B\u524D\uFF09\u3002\u4F7F\u7528\u4E00\u4E2A\u5728\u6B63\u6587\u4E2D\u7F55\u89C1\u7684\u5B57\u7B26\u3002",
  "Color-only display": "\u7EAF\u989C\u8272\u663E\u793A",
  "When enabled, virtual links are shown in a custom text color instead of the default background shadow.": "\u542F\u7528\u540E\uFF0C\u865A\u62DF\u94FE\u63A5\u4EE5\u81EA\u5B9A\u4E49\u6587\u672C\u989C\u8272\u663E\u793A\uFF0C\u800C\u975E\u9ED8\u8BA4\u80CC\u666F\u9634\u5F71\u3002",
  "Virtual link color": "\u865A\u62DF\u94FE\u63A5\u989C\u8272",
  "Custom CSS color for virtual links (e.g., #409eff, var(--text-accent)). Only used when color-only display is enabled. (Restart plugin to apply)": "\u81EA\u5B9A\u4E49\u865A\u62DF\u94FE\u63A5\u7684 CSS \u989C\u8272\uFF08\u5982 #409eff\u3001var(--text-accent)\uFF09\u3002\u4EC5\u5728\u7EAF\u989C\u8272\u663E\u793A\u6A21\u5F0F\u4E0B\u751F\u6548\u3002\uFF08\u9700\u91CD\u542F\u63D2\u4EF6\u751F\u6548\uFF09",
  "Alternative display style": "\u66FF\u4EE3\u663E\u793A\u6837\u5F0F",
  "When enabled, strikethrough is replaced with underline, and %%comments%% are collapsed into small dots that expand on the active line.": "\u542F\u7528\u540E\uFF0C\u5220\u9664\u7EBF\u88AB\u66FF\u6362\u4E3A\u4E0B\u5212\u7EBF\uFF0C%%\u6CE8\u91CA%%\u6298\u53E0\u4E3A\u5C0F\u5706\u70B9\uFF0C\u5149\u6807\u6240\u5728\u884C\u81EA\u52A8\u5C55\u5F00\u3002",
  "Always show multiple references": "\u59CB\u7EC8\u663E\u793A\u591A\u4E2A\u5F15\u7528",
  "If toggled, if there are multiple matching notes, all references are shown behind the match. If not toggled, the references are only shown if hovering over the match.": "\u542F\u7528\u540E\uFF0C\u5F53\u5B58\u5728\u591A\u4E2A\u5339\u914D\u7B14\u8BB0\u65F6\uFF0C\u6240\u6709\u5F15\u7528\u90FD\u4F1A\u663E\u793A\u5728\u5339\u914D\u9879\u540E\u9762\u3002\u5173\u95ED\u65F6\u4EC5\u5728\u60AC\u505C\u65F6\u663E\u793A\u3002",
  "Virtual link suffix": "\u865A\u62DF\u94FE\u63A5\u540E\u7F00",
  "The suffix to add to auto generated virtual links.": "\u81EA\u52A8\u751F\u6210\u7684\u865A\u62DF\u94FE\u63A5\u540E\u6DFB\u52A0\u7684\u540E\u7F00\u3002",
  "Virtual link suffix for aliases": "\u522B\u540D\u865A\u62DF\u94FE\u63A5\u540E\u7F00",
  "The suffix to add to auto generated virtual links for aliases.": "\u522B\u540D\u81EA\u52A8\u751F\u6210\u7684\u865A\u62DF\u94FE\u63A5\u540E\u6DFB\u52A0\u7684\u540E\u7F00\u3002",
  "Apply default link styling": "\u5E94\u7528\u9ED8\u8BA4\u94FE\u63A5\u6837\u5F0F",
  "If toggled, the default link styling will be applied to virtual links. Furthermore, you can style the links yourself with a CSS-snippet affecting the class `virtual-link`. (Find the CSS snippet directory at Appearance -> CSS Snippets -> Open snippets folder)": "\u542F\u7528\u540E\uFF0C\u9ED8\u8BA4\u94FE\u63A5\u6837\u5F0F\u5C06\u5E94\u7528\u4E8E\u865A\u62DF\u94FE\u63A5\u3002\u4F60\u4E5F\u53EF\u4EE5\u901A\u8FC7 CSS \u4EE3\u7801\u7247\u6BB5\u81EA\u884C\u8BBE\u7F6E `virtual-link` \u7C7B\u7684\u6837\u5F0F\u3002",
  "Use default link style for conversion": "\u8F6C\u6362\u65F6\u4F7F\u7528\u9ED8\u8BA4\u94FE\u63A5\u6837\u5F0F",
  "If toggled, the default link style will be used for the conversion of virtual links to real links.": "\u542F\u7528\u540E\uFF0C\u5C06\u865A\u62DF\u94FE\u63A5\u8F6C\u6362\u4E3A\u771F\u5B9E\u94FE\u63A5\u65F6\u5C06\u4F7F\u7528\u9ED8\u8BA4\u94FE\u63A5\u6837\u5F0F\u3002",
  "Use [[wikilinks]]": "\u4F7F\u7528 [[\u53CC\u94FE]]",
  "If toggled, the virtual links will be created as wikilinks instead of Markdown links.": "\u542F\u7528\u540E\uFF0C\u865A\u62DF\u94FE\u63A5\u5C06\u4F7F\u7528\u53CC\u94FE\u683C\u5F0F\u800C\u975E Markdown \u94FE\u63A5\u683C\u5F0F\u3002",
  "Link format": "\u94FE\u63A5\u683C\u5F0F",
  "The format of the generated links.": "\u751F\u6210\u94FE\u63A5\u7684\u683C\u5F0F\u3002"
};
function t(text) {
  if (lang && lang.toLowerCase().startsWith("zh")) {
    return zhCN[text] || text;
  }
  return text;
}

// linker/readModeLinker.ts
var import_obsidian4 = require("obsidian");

// linker/linkerCache.ts
var import_obsidian2 = require("obsidian");

// linker/linkerInfo.ts
var import_obsidian = require("obsidian");
var LinkerFileMetaInfo = class {
  constructor(fetcher, file) {
    this.fetcher = fetcher;
    this.fetcher = fetcher;
    this.file = file instanceof import_obsidian.TFile ? file : this.fetcher.app.vault.getFileByPath(file.path);
    const settings = this.fetcher.settings;
    const fileCache = this.fetcher.app.metadataCache.getFileCache(this.file);
    this.tags = (fileCache ? (0, import_obsidian.getAllTags)(fileCache) : []).filter((tag) => tag.trim().length > 0).map((tag) => tag.startsWith("#") ? tag.slice(1) : tag);
    this.includeFile = this.tags.includes(settings.tagToIncludeFile);
    this.excludeFile = this.tags.includes(settings.tagToExcludeFile);
    this.includeAllFiles = fetcher.includeAllFiles;
    this.isInIncludedDir = fetcher.includeDirPattern.test(this.file.path);
    this.isInExcludedDir = fetcher.excludeDirPattern.test(this.file.path);
  }
};
var LinkerMetaInfoFetcher = class {
  constructor(app, settings) {
    this.app = app;
    this.settings = settings;
    this.refreshSettings();
  }
  refreshSettings(settings) {
    this.settings = settings != null ? settings : this.settings;
    this.includeAllFiles = this.settings.includeAllFiles;
    this.includeDirPattern = new RegExp(`(^|/)(${this.settings.linkerDirectories.join("|")})/`);
    this.excludeDirPattern = new RegExp(`(^|/)(${this.settings.excludedDirectories.join("|")})/`);
  }
  getMetaInfo(file) {
    return new LinkerFileMetaInfo(this, file);
  }
};

// linker/linkerCache.ts
var _ExternalUpdateManager = class {
  constructor() {
    this.registeredCallbacks = /* @__PURE__ */ new Set();
  }
  registerCallback(callback) {
    this.registeredCallbacks.add(callback);
  }
  update() {
    window.setTimeout(() => {
      for (const callback of this.registeredCallbacks) {
        callback();
      }
    }, _ExternalUpdateManager.UPDATE_DELAY_MS);
  }
};
var ExternalUpdateManager = _ExternalUpdateManager;
ExternalUpdateManager.UPDATE_DELAY_MS = 50;
var PrefixNode = class {
  constructor() {
    this.children = /* @__PURE__ */ new Map();
    this.files = /* @__PURE__ */ new Set();
    this.charValue = "";
    this.depth = 0;
    this.requiresCaseMatch = false;
  }
};
var VisitedPrefixNode = class {
  constructor(node, caseIsMatched = true, startedAtWordBeginning = false) {
    this.formattingDelta = 0;
    this.node = node;
    this.caseIsMatched = caseIsMatched;
    this.startedAtWordBeginning = startedAtWordBeginning;
  }
};
var MatchNode = class {
  constructor() {
    this.start = 0;
    this.length = 0;
    this.files = /* @__PURE__ */ new Set();
    this.value = "";
    this.type = 0 /* Note */;
    this.caseIsMatched = true;
    this.startsAtWordBoundary = false;
    this.requiresCaseMatch = false;
  }
  get end() {
    return this.start + this.length;
  }
  get isAlias() {
    return this.type === 1 /* Alias */;
  }
};
var _PrefixTree = class {
  constructor(app, settings) {
    this.app = app;
    this.settings = settings;
    this.root = new PrefixNode();
    this._currentNodes = [];
    this.setIndexedFilePaths = /* @__PURE__ */ new Set();
    this.mapIndexedFilePathsToUpdateTime = /* @__PURE__ */ new Map();
    this.mapFilePathToLeaveNodes = /* @__PURE__ */ new Map();
    this.mapFileHeaderIds = /* @__PURE__ */ new Map();
    this.fetcher = new LinkerMetaInfoFetcher(this.app, this.settings);
    this.updateTree();
  }
  clear() {
    this.root = new PrefixNode();
    this._currentNodes = [];
    this.setIndexedFilePaths.clear();
    this.mapIndexedFilePathsToUpdateTime.clear();
    this.mapFilePathToLeaveNodes.clear();
  }
  isExcluded(value) {
    const valueLower = value.toLowerCase();
    return this.settings.excludedKeywords.some((kw) => kw.toLowerCase() === valueLower);
  }
  getCurrentMatchNodes(index, excludedNote, specificFile) {
    const matchNodes = [];
    const allFiles = /* @__PURE__ */ new Set();
    if (excludedNote === void 0 && this.settings.excludeLinksToOwnNote) {
      excludedNote = this.app.workspace.getActiveFile();
    }
    for (const node of this._currentNodes) {
      const valueString = this.getNodeValue(node.node);
      if (node.node.files.size === 0 || this.isExcluded(valueString)) {
        continue;
      }
      const matchNode = new MatchNode();
      matchNode.length = node.node.depth + node.formattingDelta;
      matchNode.start = index - matchNode.length;
      if (specificFile) {
        matchNode.files = new Set(Array.from(node.node.files).filter((file) => file.path === specificFile.path));
      } else {
        matchNode.files = new Set(Array.from(node.node.files).filter((file) => !excludedNote || file.path !== excludedNote.path));
      }
      matchNode.files.forEach((f) => allFiles.add(f));
      if (this.settings.maxReferencesToHideLink > 0 && allFiles.size > this.settings.maxReferencesToHideLink) {
        return [];
      }
      matchNode.value = valueString;
      matchNode.requiresCaseMatch = node.node.requiresCaseMatch;
      const fileNames = Array.from(matchNode.files).map((file) => file.basename);
      const nodeValue = valueString;
      if (fileNames.map((n) => n.toLowerCase()).includes(nodeValue.toLowerCase())) {
        matchNode.type = 0 /* Note */;
      } else {
        const file = Array.from(matchNode.files)[0];
        if (file) {
          const metadata = this.app.metadataCache.getFileCache(file);
          let headingMatch = null;
          if (metadata == null ? void 0 : metadata.headings) {
            if (this.settings.headerMatchSymbols && this.settings.headerMatchStartSymbol && this.settings.headerMatchEndSymbol && this.settings.headerMatchStartSymbol !== this.settings.headerMatchEndSymbol) {
              for (const h of metadata.headings) {
                const headingText = h.heading;
                const startSymbol = this.settings.headerMatchStartSymbol;
                const endSymbol = this.settings.headerMatchEndSymbol;
                let searchStartIndex = 0;
                while (searchStartIndex < headingText.length) {
                  const startIndex = headingText.indexOf(startSymbol, searchStartIndex);
                  if (startIndex === -1)
                    break;
                  const afterStartIndex = startIndex + startSymbol.length;
                  const endIndex = headingText.indexOf(endSymbol, afterStartIndex);
                  if (endIndex === -1)
                    break;
                  if (startIndex < endIndex) {
                    const keyword = headingText.substring(startIndex + startSymbol.length, endIndex).trim();
                    if (keyword.toLowerCase() === nodeValue.toLowerCase()) {
                      headingMatch = h;
                      break;
                    }
                    searchStartIndex = endIndex + endSymbol.length;
                  } else {
                    searchStartIndex = afterStartIndex;
                  }
                }
                if (headingMatch)
                  break;
              }
              if (!headingMatch && !this.settings.headerMatchOnlyBetweenSymbols) {
                headingMatch = metadata.headings.find((h) => h.heading.toLowerCase() === nodeValue.toLowerCase());
              }
            } else {
              headingMatch = metadata.headings.find((h) => h.heading.toLowerCase() === nodeValue.toLowerCase());
            }
          }
          if (headingMatch) {
            matchNode.type = 2 /* Header */;
            matchNode.headerId = headingMatch.heading.trim();
          } else if (fileNames.map((n) => n.toLowerCase()).includes(nodeValue.toLowerCase())) {
            matchNode.type = 0 /* Note */;
          } else {
            matchNode.type = 1 /* Alias */;
          }
        } else {
          matchNode.type = 1 /* Alias */;
        }
      }
      let currentNode = node.node;
      while (currentNode) {
        if (!node.caseIsMatched) {
          matchNode.caseIsMatched = false;
          break;
        }
        currentNode = currentNode.parent;
      }
      matchNode.startsAtWordBoundary = node.startedAtWordBeginning;
      if (matchNode.requiresCaseMatch && !matchNode.caseIsMatched) {
        continue;
      }
      if (matchNode.files.size > 0) {
        if (matchNode.type === 2 /* Header */) {
          const activeFile = this.app.workspace.getActiveFile();
          if (activeFile) {
            matchNode.files = new Set(Array.from(matchNode.files).filter((f) => f.path !== activeFile.path));
          }
        }
        if (matchNode.files.size > 0) {
          if (matchNode.type === 2 /* Header */ && !matchNode.headerId) {
            for (const f of matchNode.files) {
              const headerId = this.getFileHeaderId(f, nodeValue);
              if (headerId) {
                matchNode.headerId = headerId;
                break;
              }
            }
          }
          matchNodes.push(matchNode);
        }
      }
    }
    matchNodes.sort((a, b) => b.length - a.length);
    return matchNodes;
  }
  addFileWithName(name, file, matchCase, headerId) {
    var _a2, _b2;
    let node = this.root;
    for (let char of name) {
      let child = node.children.get(char);
      if (!child) {
        child = new PrefixNode();
        child.parent = node;
        child.charValue = char;
        child.depth = node.depth + 1;
        node.children.set(char, child);
      }
      node = child;
    }
    node.files.add(file);
    node.requiresCaseMatch = matchCase;
    if (headerId) {
      const existingIds = (_a2 = this.mapFileHeaderIds.get(file.path)) != null ? _a2 : /* @__PURE__ */ new Map();
      existingIds.set(name, headerId);
      this.mapFileHeaderIds.set(file.path, existingIds);
    }
    const path = file.path;
    this.mapFilePathToLeaveNodes.set(path, [node, ...(_b2 = this.mapFilePathToLeaveNodes.get(path)) != null ? _b2 : []]);
  }
  getFileHeaderId(file, keyword) {
    var _a2;
    return (_a2 = this.mapFileHeaderIds.get(file.path)) == null ? void 0 : _a2.get(keyword);
  }
  getNodeValue(node) {
    const chars = [];
    let current = node;
    while (current && current !== this.root) {
      if (current.charValue)
        chars.push(current.charValue);
      current = current.parent;
    }
    return chars.reverse().join("");
  }
  static isNoneEmptyString(value) {
    return value !== null && value !== void 0 && typeof value === "string" && value.trim().length > 0;
  }
  static isUpperCaseString(value, upperCasePart = 0.75) {
    if (!_PrefixTree.isNoneEmptyString(value)) {
      return false;
    }
    const length = value.length;
    const upperCaseChars = [...value].filter((char) => char.toLowerCase() !== char.toUpperCase() && char === char.toUpperCase()).length;
    return upperCaseChars / length >= upperCasePart;
  }
  addFileToTree(file) {
    var _a2, _b2, _c, _d, _e, _f, _g;
    const path = file.path;
    if (!file || !path) {
      return;
    }
    if (this.settings.excludedExtensions.some((ext) => path.toLowerCase().endsWith(ext.toLowerCase()))) {
      return;
    }
    this.removeFileFromTree(file);
    this.setIndexedFilePaths.add(path);
    this.mapIndexedFilePathsToUpdateTime.set(path, file.stat.mtime);
    const metaInfo = this.fetcher.getMetaInfo(file);
    const fileCache = this.app.metadataCache.getFileCache(file);
    const tagsArray = fileCache ? (0, import_obsidian2.getAllTags)(fileCache) : null;
    const tags = (tagsArray != null ? tagsArray : []).filter((s) => _PrefixTree.isNoneEmptyString(s)).map((tag) => tag.startsWith("#") ? tag.slice(1) : tag);
    const includeFile = metaInfo.includeFile;
    const excludeFile = metaInfo.excludeFile;
    const isInIncludedDir = metaInfo.isInIncludedDir;
    const isInExcludedDir = metaInfo.isInExcludedDir;
    if (excludeFile || isInExcludedDir && !includeFile) {
      return;
    }
    if (!includeFile && !isInIncludedDir && !metaInfo.includeAllFiles) {
      return;
    }
    const metadata = this.app.metadataCache.getFileCache(file);
    let aliases = (_b2 = (_a2 = metadata == null ? void 0 : metadata.frontmatter) == null ? void 0 : _a2.aliases) != null ? _b2 : [];
    let headerEntries = [];
    if (this.settings.includeHeaders && (metadata == null ? void 0 : metadata.headings)) {
      const canMatchSymbols = this.settings.headerMatchSymbols && this.settings.headerMatchStartSymbol && this.settings.headerMatchEndSymbol && this.settings.headerMatchStartSymbol !== this.settings.headerMatchEndSymbol;
      if (canMatchSymbols) {
        const symbolKeywords = /* @__PURE__ */ new Set();
        for (const h of metadata.headings) {
          const headingText = h.heading;
          const startSymbol = this.settings.headerMatchStartSymbol;
          const endSymbol = this.settings.headerMatchEndSymbol;
          let searchStartIndex = 0;
          while (searchStartIndex < headingText.length) {
            const startIndex = headingText.indexOf(startSymbol, searchStartIndex);
            if (startIndex === -1)
              break;
            const afterStartIndex = startIndex + startSymbol.length;
            const endIndex = headingText.indexOf(endSymbol, afterStartIndex);
            if (endIndex === -1)
              break;
            if (startIndex < endIndex) {
              const keyword = headingText.substring(startIndex + startSymbol.length, endIndex).trim();
              if (keyword) {
                headerEntries.push({ keyword, headerId: h.heading.replace(/\s+/g, "-").toLowerCase() });
                symbolKeywords.add(keyword);
              }
              searchStartIndex = endIndex + endSymbol.length;
            } else {
              searchStartIndex = afterStartIndex;
            }
          }
        }
        if (!this.settings.headerMatchOnlyBetweenSymbols) {
          for (const h of metadata.headings) {
            if (!symbolKeywords.has(h.heading)) {
              headerEntries.push({ keyword: h.heading, headerId: h.heading.replace(/\s+/g, "-").toLowerCase() });
            }
          }
        }
      } else {
        headerEntries = metadata.headings.map((h) => ({ keyword: h.heading, headerId: h.heading.replace(/\s+/g, "-").toLowerCase() }));
      }
    }
    let aliasesWithMatchCase = new Set((_d = (_c = metadata == null ? void 0 : metadata.frontmatter) == null ? void 0 : _c[this.settings.propertyNameToMatchCase]) != null ? _d : []);
    let aliasesWithIgnoreCase = new Set((_f = (_e = metadata == null ? void 0 : metadata.frontmatter) == null ? void 0 : _e[this.settings.propertyNameToIgnoreCase]) != null ? _f : []);
    if (!Array.isArray(aliases)) {
      aliases = [aliases];
    }
    try {
      aliases = aliases.filter((s) => _PrefixTree.isNoneEmptyString(s));
    } catch (e) {
    }
    let names = [file.basename];
    if (aliases && this.settings.includeAliases) {
      names.push(...aliases);
    }
    if (headerEntries.length > 0 && this.settings.includeHeaders) {
      names.push(...headerEntries.map((e) => e.keyword));
    }
    names = names.filter((s) => _PrefixTree.isNoneEmptyString(s));
    let namesWithCaseIgnore = new Array();
    let namesWithCaseMatch = new Array();
    if (this.settings.matchCaseSensitive) {
      let lowerCaseNames = new Array();
      if (tags.includes(this.settings.tagToIgnoreCase)) {
        namesWithCaseIgnore = [...names];
      } else {
        namesWithCaseMatch = [...names];
      }
      lowerCaseNames = lowerCaseNames.map((name) => name.toLowerCase());
      names.push(...lowerCaseNames);
    } else {
      if (tags.includes(this.settings.tagToMatchCase)) {
        namesWithCaseMatch = [...names];
      } else {
        const prop = this.settings.capitalLetterProportionForAutomaticMatchCase;
        namesWithCaseMatch = [...names].filter((name) => _PrefixTree.isUpperCaseString(name, prop) && !aliasesWithIgnoreCase.has(name));
        namesWithCaseIgnore = [...names].filter((name) => !namesWithCaseMatch.includes(name));
      }
    }
    const namesToMoveFromIgnoreToMatch = namesWithCaseIgnore.filter((name) => aliasesWithMatchCase.has(name));
    const namesToMoveFromMatchToIgnore = namesWithCaseMatch.filter((name) => aliasesWithIgnoreCase.has(name));
    namesWithCaseIgnore = namesWithCaseIgnore.filter((name) => !namesToMoveFromIgnoreToMatch.includes(name));
    namesWithCaseMatch = namesWithCaseMatch.filter((name) => !namesToMoveFromMatchToIgnore.includes(name));
    namesWithCaseIgnore.push(...namesToMoveFromMatchToIgnore);
    namesWithCaseMatch.push(...namesToMoveFromIgnoreToMatch);
    namesWithCaseIgnore.push(...namesWithCaseIgnore.map((name) => name.toLowerCase()));
    namesWithCaseIgnore = namesWithCaseIgnore.filter((name) => !this.isExcluded(name));
    namesWithCaseMatch = namesWithCaseMatch.filter((name) => !this.isExcluded(name));
    namesWithCaseIgnore.forEach((name) => {
      this.addFileWithName(name, file, false);
    });
    namesWithCaseMatch.forEach((name) => {
      this.addFileWithName(name, file, true);
    });
    for (const entry of headerEntries) {
      if (entry.headerId) {
        const existingIds = (_g = this.mapFileHeaderIds.get(file.path)) != null ? _g : /* @__PURE__ */ new Map();
        existingIds.set(entry.keyword, entry.headerId);
        this.mapFileHeaderIds.set(file.path, existingIds);
      }
    }
  }
  removeFileFromTree(file) {
    var _a2;
    const path = typeof file === "string" ? file : file.path;
    const nodes = (_a2 = this.mapFilePathToLeaveNodes.get(path)) != null ? _a2 : [];
    for (const node of nodes) {
      node.files = new Set([...node.files].filter((f) => f.path !== path));
    }
    for (let i = nodes.length - 1; i >= 0; i--) {
      const node = nodes[i];
      let currentNode = node;
      while (currentNode.files.size === 0 && currentNode.children.size === 0) {
        const parent = currentNode.parent;
        if (!parent || parent === this.root) {
          break;
        }
        parent.children.delete(currentNode.charValue);
        currentNode = parent;
      }
    }
    this.setIndexedFilePaths.delete(path);
    this.mapFilePathToLeaveNodes.delete(path);
    this.mapFileHeaderIds.delete(path);
    this.mapIndexedFilePathsToUpdateTime.delete(path);
  }
  fileIsUpToDate(file) {
    const mtime = file.stat.mtime;
    const path = file.path;
    return this.mapIndexedFilePathsToUpdateTime.has(path) && this.mapIndexedFilePathsToUpdateTime.get(path) === mtime;
  }
  updateTree(updateFiles) {
    this.fetcher.refreshSettings();
    const currentVaultFiles = /* @__PURE__ */ new Set();
    let files = new Array();
    const allFiles = this.app.vault.getFiles().filter((file) => {
      const ext = file.extension.toLowerCase();
      return _PrefixTree.SUPPORTED_EXTENSIONS.includes(ext);
    });
    allFiles.forEach((f) => currentVaultFiles.add(f.path));
    if (allFiles.length !== this.setIndexedFilePaths.size || !(updateFiles == null ? void 0 : updateFiles.length)) {
      files = allFiles;
    } else {
      files = updateFiles.map((f) => f ? this.app.vault.getAbstractFileByPath(f) : null).filter((f) => f instanceof import_obsidian2.TFile);
    }
    for (const file of files) {
      if (this.fileIsUpToDate(file)) {
        continue;
      }
      try {
        this.addFileToTree(file);
      } catch (e) {
      }
    }
    const filesToRemove = [...this.setIndexedFilePaths].filter((f) => !currentVaultFiles.has(f));
    filesToRemove.forEach((f) => this.removeFileFromTree(f));
  }
  findFiles(prefix) {
    let node = this.root;
    for (const char of prefix) {
      node = node.children.get(char.toLowerCase());
      if (!node) {
        return /* @__PURE__ */ new Set();
      }
    }
    return node.files;
  }
  resetSearch() {
    this._currentNodes = [new VisitedPrefixNode(this.root)];
  }
  pushChar(char) {
    const newNodes = [];
    const chars = [char, char.toLowerCase()];
    chars.forEach((c) => {
      const isBoundary = _PrefixTree.checkWordBoundary(c);
      if (this.settings.matchAnyPartsOfWords || isBoundary || this.settings.matchEndOfWords) {
        newNodes.push(new VisitedPrefixNode(this.root, true, isBoundary));
      }
      for (const node of this._currentNodes) {
        const child = node.node.children.get(c);
        const startedAtBoundary = node.startedAtWordBeginning;
        if (child) {
          const newPrefixNodes = newNodes.map((n) => n.node);
          if (!newPrefixNodes.includes(child)) {
            const newVisited = new VisitedPrefixNode(child, char == c, startedAtBoundary);
            newVisited.formattingDelta = node.formattingDelta;
            newNodes.push(newVisited);
          }
        }
      }
    });
    this._currentNodes = newNodes;
  }
  static checkWordBoundary(char) {
    let pattern = /[^\p{L}]/u;
    return pattern.test(char);
  }
  static isFormattingChar(char) {
    const pattern = /[^\p{L}\p{N}]/u;
    return pattern.test(char);
  }
};
var PrefixTree = _PrefixTree;
PrefixTree.SUPPORTED_EXTENSIONS = [
  "md",
  "png",
  "jpg",
  "jpeg",
  "gif",
  "svg",
  "pdf",
  "doc",
  "docx",
  "xls",
  "xlsx",
  "mp3",
  "wav",
  "ogg",
  "mp4",
  "mov",
  "avi",
  "webm"
];
var LinkerCache = class {
  constructor(app, settings) {
    this.app = app;
    this.settings = settings;
    const { vault } = app;
    this.vault = vault;
    this.cache = new PrefixTree(app, settings);
    this.updateCache(true);
  }
  static getInstance(app, settings) {
    if (!LinkerCache.instance) {
      LinkerCache.instance = new LinkerCache(app, settings);
    }
    return LinkerCache.instance;
  }
  clearCache() {
    this.cache.clear();
  }
  reset() {
    this.cache.resetSearch();
  }
  updateCache(force = false) {
    var _a2, _b2, _c;
    if (!this.settings.linkerActivated)
      return;
    if (!((_b2 = (_a2 = this.app) == null ? void 0 : _a2.workspace) == null ? void 0 : _b2.getActiveFile())) {
      return;
    }
    const activeFile = (_c = this.app.workspace.getActiveFile()) == null ? void 0 : _c.path;
    if (activeFile === this.activeFilePath && !force) {
      return;
    }
    this.cache.updateTree(force ? void 0 : [activeFile, this.activeFilePath]);
    this.activeFilePath = activeFile;
  }
};

// linker/virtualLinkDom.ts
var import_obsidian3 = require("obsidian");
var VirtualMatch = class {
  constructor(id, originText, from, to, files, type, isSubWord, settings, plugin, headerId, isBoldContext = false, isItalicContext = false, isHighlightContext = false, isTripleStarContext = false, isStrikethroughContext = false, isCommentContext = false) {
    this.id = id;
    this.originText = originText;
    this.from = from;
    this.to = to;
    this.files = files;
    this.type = type;
    this.isSubWord = isSubWord;
    this.settings = settings;
    this.plugin = plugin;
    this.headerId = headerId;
    this.isBoldContext = isBoldContext;
    this.isItalicContext = isItalicContext;
    this.isHighlightContext = isHighlightContext;
    this.isTripleStarContext = isTripleStarContext;
    this.isStrikethroughContext = isStrikethroughContext;
    this.isCommentContext = isCommentContext;
    this.fileHeaderIds = /* @__PURE__ */ new Map();
    if (headerId) {
      for (const file of files) {
        this.fileHeaderIds.set(file.path, headerId);
      }
    }
  }
  setFileHeaderId(file, headerId) {
    this.fileHeaderIds.set(file.path, headerId);
  }
  getFileHeaderId(file) {
    return this.fileHeaderIds.get(file.path);
  }
  get isAlias() {
    return this.type === 1 /* Alias */;
  }
  getCompleteLinkElement(inTableCellEditor = false) {
    if (this.settings.maxReferencesToHideLink > 0 && this.files.length > this.settings.maxReferencesToHideLink) {
      const emptySpan = activeDocument.createElement("span");
      emptySpan.textContent = this.originText;
      return emptySpan;
    }
    const sortedFiles = [...this.files].sort((a, b) => {
      const typeA = this.getFileTypeOrder(a);
      const typeB = this.getFileTypeOrder(b);
      return typeA - typeB;
    });
    const visibleFiles = this.settings.maxReferenceCount > 0 ? sortedFiles.slice(0, this.settings.maxReferenceCount) : sortedFiles;
    const span = this.getLinkRootSpan(inTableCellEditor);
    const firstFile = visibleFiles.length > 0 ? visibleFiles[0] : void 0;
    const firstPath = firstFile ? (0, import_obsidian3.getLinkpath)(firstFile.path) : "";
    span.appendChild(this.getLinkAnchorElement(this.originText, firstPath, firstFile));
    if (visibleFiles.length > 1) {
      if (!this.isSubWord) {
        span.appendChild(this.getMultipleReferencesIndicatorSpan());
      }
      span.appendChild(this.getMultipleReferencesSpan(visibleFiles));
    }
    if (!this.isSubWord || !this.settings.suppressSuffixForSubWords) {
      const icon = this.getIconSpan();
      if (icon)
        span.appendChild(icon);
    }
    return span;
  }
  getFileTypeOrder(file) {
    if (this.fileHeaderIds.has(file.path))
      return 2;
    const keyword = this.originText;
    if (file.basename.toLowerCase() === keyword.toLowerCase())
      return 0;
    if (file.basename.includes(keyword))
      return 0;
    return 1;
  }
  getLinkAnchorElement(linkText, href, file) {
    const link = activeDocument.createElement("a");
    let headerIdToUse;
    if (file) {
      headerIdToUse = this.getFileHeaderId(file);
    } else if (this.files.length > 0) {
      headerIdToUse = this.getFileHeaderId(this.files[0]) || this.headerId;
    } else {
      headerIdToUse = this.headerId;
    }
    let fullPath = href;
    if (headerIdToUse) {
      link.href = `${href}#${headerIdToUse}`;
      link.setAttribute("data-heading-id", headerIdToUse);
      fullPath = `${href}#${headerIdToUse}`;
    } else {
      link.href = href;
    }
    link.textContent = linkText;
    link.target = "_blank";
    link.rel = "noopener noreferrer";
    link.setAttribute("from", this.from.toString());
    link.setAttribute("to", this.to.toString());
    link.setAttribute("origin-text", this.originText);
    link.classList.add("internal-link", "virtual-link-a");
    link.onclick = (event) => {
      event.preventDefault();
      event.stopPropagation();
      const targetFile = file || (this.files.length > 0 ? this.files[0] : null);
      if (!targetFile)
        return false;
      if (this.plugin && this.plugin.app) {
        void this.plugin.app.workspace.openLinkText(fullPath, "", false, { active: true });
        if (headerIdToUse) {
          const refullPath = fullPath;
          [500, 3e3, 6e3].forEach((delay) => {
            window.setTimeout(() => {
              void this.plugin.app.workspace.openLinkText(refullPath, "", false, { active: true });
            }, delay);
          });
        }
      }
      return false;
    };
    return link;
  }
  getLinkRootSpan(inTableCellEditor = false) {
    const span = activeDocument.createElement("span");
    span.classList.add("glossary-entry", "virtual-link", "virtual-link-span");
    if (this.settings.applyDefaultLinkStyling) {
      span.classList.add("virtual-link-default");
    }
    if (this.isBoldContext) {
      span.classList.add("virtual-link-in-bold");
    }
    if (this.isItalicContext) {
      span.classList.add("virtual-link-in-italic");
    }
    if (this.isHighlightContext) {
      span.classList.add("virtual-link-in-highlight");
    } else {
      let parent = span.parentElement;
      while (parent) {
        if (parent.tagName === "MARK") {
          span.classList.add("virtual-link-in-highlight");
          break;
        }
        parent = parent.parentElement;
      }
    }
    if (this.isTripleStarContext) {
      span.classList.add("virtual-link-in-triple-star");
    }
    if (this.isStrikethroughContext) {
      span.classList.add("virtual-link-in-strikethrough");
    }
    if (inTableCellEditor === true) {
      span.classList.add("no-context-menu");
      span.addEventListener("contextmenu", (e) => {
        e.preventDefault();
        e.stopPropagation();
        return false;
      }, true);
      span.addEventListener("mouseup", (e) => {
        if (e.button === 2) {
          e.preventDefault();
          e.stopPropagation();
          return false;
        }
      }, true);
    }
    return span;
  }
  getMultipleReferencesSpan(files) {
    const spanReferences = activeDocument.createElement("span");
    if (!this.settings.alwaysShowMultipleReferences) {
      spanReferences.classList.add("multiple-files-references");
    }
    const fileList = files != null ? files : this.files;
    if (!fileList || fileList.length === 0) {
      return spanReferences;
    }
    fileList.forEach((file, index) => {
      if (index === 0) {
        const bracket = activeDocument.createElement("span");
        bracket.textContent = "[";
        spanReferences.appendChild(bracket);
      }
      let linkText = ` ${index + 1} `;
      if (index < fileList.length - 1) {
        linkText += "|";
      }
      let linkHref = file.path;
      const link = this.getLinkAnchorElement(linkText, linkHref, file);
      spanReferences.appendChild(link);
      if (index == fileList.length - 1) {
        const bracket = activeDocument.createElement("span");
        bracket.textContent = "]";
        spanReferences.appendChild(bracket);
      }
    });
    return spanReferences;
  }
  getMultipleReferencesIndicatorSpan() {
    const spanIndicator = activeDocument.createElement("span");
    spanIndicator.textContent = " [...]";
    spanIndicator.classList.add("multiple-files-indicator");
    return spanIndicator;
  }
  getIconSpan() {
    var _a2;
    const suffix = this.isAlias ? this.settings.virtualLinkAliasSuffix : this.settings.virtualLinkSuffix;
    if (((_a2 = suffix == null ? void 0 : suffix.length) != null ? _a2 : 0) > 0) {
      let icon = activeDocument.createElement("sup");
      icon.textContent = suffix;
      icon.classList.add("linker-suffix-icon");
      return icon;
    }
    return null;
  }
  static compare(a, b) {
    if (a.from === b.from) {
      if (b.to == a.to) {
        return b.files.length - a.files.length;
      }
      return b.to - a.to;
    }
    return a.from - b.from;
  }
  static sort(matches) {
    return Array.from(matches).sort((a, b) => VirtualMatch.compare(a, b));
  }
  static filterAlreadyLinked(matches, linkedFiles, mode = "every") {
    return matches.filter((match) => {
      if (mode === "every") {
        return !match.files.every((file) => linkedFiles.has(file));
      } else {
        return !match.files.some((file) => linkedFiles.has(file));
      }
    });
  }
  static filterOverlapping(matches, onlyLinkOnce = true, excludedIntervalTree) {
    const matchesToDelete = /* @__PURE__ */ new Map();
    for (let i = 0; i < matches.length; i++) {
      const addition = matches[i];
      if (matchesToDelete.has(addition.id)) {
        continue;
      }
      if (excludedIntervalTree) {
        const overlaps = excludedIntervalTree.search([addition.from, addition.to]);
        if (overlaps.length > 0) {
          matchesToDelete.set(addition.id, true);
          continue;
        }
      }
      for (let j = i + 1; j < matches.length; j++) {
        const otherAddition = matches[j];
        if (otherAddition.from >= addition.to) {
          break;
        }
        matchesToDelete.set(otherAddition.id, true);
      }
      if (onlyLinkOnce) {
        for (let j = i + 1; j < matches.length; j++) {
          const otherAddition = matches[j];
          if (matchesToDelete.has(otherAddition.id)) {
            continue;
          }
          if (otherAddition.files.every((f) => addition.files.contains(f))) {
            matchesToDelete.set(otherAddition.id, true);
          }
        }
      }
    }
    return matches.filter((match) => !matchesToDelete.has(match.id));
  }
};

// linker/readModeLinker.ts
var GlossaryLinker = class extends import_obsidian4.MarkdownRenderChild {
  constructor(app, settings, context, containerEl, plugin) {
    super(containerEl);
    this.plugin = plugin;
    this.settings = settings;
    this.app = app;
    this.ctx = context;
    this.linkerCache = LinkerCache.getInstance(app, settings);
    this.load();
  }
  clearExistingLinks() {
    const virtualLinks = this.containerEl.querySelectorAll(".virtual-link");
    virtualLinks.forEach((link) => {
      const anchor = link.querySelector(".virtual-link-a");
      const originalText = (anchor == null ? void 0 : anchor.getAttribute("origin-text")) || (anchor == null ? void 0 : anchor.textContent) || "";
      if (originalText) {
        const textNode = activeDocument.createTextNode(originalText);
        link.replaceWith(textNode);
      } else {
        link.remove();
      }
    });
  }
  getClosestLinkPath(glossaryName) {
    var _a2, _b2;
    const destName = this.ctx.sourcePath.replace(/(.*).md/, "$1");
    let currentDestName = destName;
    let currentPath = this.app.metadataCache.getFirstLinkpathDest((0, import_obsidian4.getLinkpath)(glossaryName), currentDestName);
    if (currentPath == null)
      return null;
    while (currentDestName.includes("/")) {
      currentDestName = currentDestName.replace(/\/[^/]*?$/, "");
      const newPath = this.app.metadataCache.getFirstLinkpathDest((0, import_obsidian4.getLinkpath)(glossaryName), currentDestName);
      if ((((_a2 = newPath == null ? void 0 : newPath.path) == null ? void 0 : _a2.length) || 0) > ((_b2 = currentPath == null ? void 0 : currentPath.path) == null ? void 0 : _b2.length)) {
        currentPath = newPath;
        break;
      }
    }
    return currentPath;
  }
  onload() {
    if (!this.settings.linkerActivated) {
      this.clearExistingLinks();
      return;
    }
    const tags = ["p", "li", "td", "th", "span", "em", "strong", "mark", "del", "s"];
    const linkedFiles = /* @__PURE__ */ new Set();
    const explicitlyLinkedFiles = /* @__PURE__ */ new Set();
    for (const tag of tags) {
      const nodeList = this.containerEl.getElementsByTagName(tag);
      for (let index = 0; index <= nodeList.length; index++) {
        const item = index == nodeList.length ? this.containerEl : nodeList.item(index);
        for (let childNodeIndex = 0; childNodeIndex < item.childNodes.length; childNodeIndex++) {
          const childNode = item.childNodes[childNodeIndex];
          if (childNode.nodeType === Node.TEXT_NODE) {
            let text = childNode.textContent || "";
            if (text.length === 0)
              continue;
            this.linkerCache.reset();
            let matches = [];
            let id = 0;
            for (let i = 0; i <= text.length; i) {
              const codePoint = text.codePointAt(i);
              const char = i < text.length ? String.fromCodePoint(codePoint) : "\n";
              const isWordBoundary = PrefixTree.checkWordBoundary(char);
              if (this.settings.matchAnyPartsOfWords || this.settings.matchBeginningOfWords || isWordBoundary) {
                const currentNodes = this.linkerCache.cache.getCurrentMatchNodes(i);
                if (currentNodes.length > 0) {
                  currentNodes.forEach((node) => {
                    if (!this.settings.matchAnyPartsOfWords) {
                      if (this.settings.matchBeginningOfWords && !node.startsAtWordBoundary && this.settings.matchEndOfWords && !isWordBoundary) {
                        return;
                      }
                    }
                    const nFrom = node.start;
                    const nTo = node.end;
                    const name = text.slice(nFrom, nTo);
                    const headerId = node.type === 2 /* Header */ ? node.headerId : void 0;
                    const match = new VirtualMatch(id++, name, nFrom, nTo, Array.from(node.files), node.type, !isWordBoundary, this.settings, this.plugin, headerId);
                    if (node.files.size > 1) {
                      node.files.forEach((file) => {
                        const fileNodes = this.linkerCache.cache.getCurrentMatchNodes(i, null, file);
                        if (fileNodes.length > 0 && fileNodes[0].headerId) {
                          match.setFileHeaderId(file, fileNodes[0].headerId);
                        }
                      });
                    }
                    const parentEl = childNode.parentElement;
                    if (parentEl) {
                      const hasSelector = (selector) => {
                        return parentEl.matches(selector) || parentEl.closest(selector) !== null;
                      };
                      match.isBoldContext = hasSelector("strong");
                      match.isItalicContext = hasSelector("em");
                      match.isHighlightContext = hasSelector("mark");
                      match.isStrikethroughContext = hasSelector("del") || hasSelector("s");
                      match.isCommentContext = hasSelector(".cm-comment");
                      match.isTripleStarContext = match.isBoldContext && match.isItalicContext;
                    }
                    matches.push(match);
                  });
                }
              }
              this.linkerCache.cache.pushChar(char);
              i += char.length;
            }
            matches = VirtualMatch.sort(matches);
            if (this.settings.excludeLinksToRealLinkedFiles) {
              matches = VirtualMatch.filterAlreadyLinked(matches, explicitlyLinkedFiles);
            }
            if (this.settings.onlyLinkOnce) {
              matches = VirtualMatch.filterAlreadyLinked(matches, linkedFiles);
            }
            matches = VirtualMatch.filterOverlapping(matches, this.settings.onlyLinkOnce);
            const parent = childNode.parentElement;
            let lastTo = 0;
            matches.forEach((match) => {
              match.files.forEach((f) => linkedFiles.add(f));
              const span = match.getCompleteLinkElement();
              if (match.from > 0) {
                parent == null ? void 0 : parent.insertBefore(activeDocument.createTextNode(text.slice(lastTo, match.from)), childNode);
              }
              parent == null ? void 0 : parent.insertBefore(span, childNode);
              let markParent = span.parentElement;
              while (markParent) {
                if (markParent.tagName === "MARK") {
                  span.classList.add("virtual-link-in-highlight");
                  break;
                }
                markParent = markParent.parentElement;
              }
              lastTo = match.to;
            });
            const textLength = text.length;
            if (lastTo < textLength) {
              parent == null ? void 0 : parent.insertBefore(activeDocument.createTextNode(text.slice(lastTo)), childNode);
            }
            parent == null ? void 0 : parent.removeChild(childNode);
            childNodeIndex += 1;
          }
        }
      }
    }
  }
};

// linker/liveLinker.ts
var import_language = require("@codemirror/language");
var import_state = require("@codemirror/state");
var import_view = require("@codemirror/view");
var import_obsidian5 = require("obsidian");

// node_modules/@flatten-js/interval-tree/dist/main.mjs
var Interval = class Interval2 {
  constructor(low, high) {
    this.low = low;
    this.high = high;
  }
  clone() {
    return new Interval2(this.low, this.high);
  }
  get max() {
    return this.clone();
  }
  less_than(other_interval) {
    return this.low < other_interval.low || this.low === other_interval.low && this.high < other_interval.high;
  }
  equal_to(other_interval) {
    return this.low === other_interval.low && this.high === other_interval.high;
  }
  intersect(other_interval) {
    return !this.not_intersect(other_interval);
  }
  not_intersect(other_interval) {
    return this.high < other_interval.low || other_interval.high < this.low;
  }
  merge(other_interval) {
    return new Interval2(this.low === void 0 ? other_interval.low : this.low < other_interval.low ? this.low : other_interval.low, this.high === void 0 ? other_interval.high : this.high > other_interval.high ? this.high : other_interval.high);
  }
  output() {
    return [this.low, this.high];
  }
  static comparable_max(interval1, interval2) {
    return interval1.merge(interval2);
  }
  static comparable_less_than(val1, val2) {
    return val1 < val2;
  }
};
var RB_TREE_COLOR_RED = 0;
var RB_TREE_COLOR_BLACK = 1;
var Node2 = class {
  constructor(key = void 0, value = void 0, left = null, right = null, parent = null, color = RB_TREE_COLOR_BLACK) {
    this.left = left;
    this.right = right;
    this.parent = parent;
    this.color = color;
    this.item = { key, value };
    if (key && key instanceof Array && key.length === 2) {
      if (!Number.isNaN(key[0]) && !Number.isNaN(key[1])) {
        let [low, high] = key;
        if (low > high)
          [low, high] = [high, low];
        this.item.key = new Interval(low, high);
      }
    }
    this.max = this.item.key ? this.item.key.max : void 0;
  }
  isNil() {
    return this.item.key === void 0 && this.item.value === void 0 && this.left === null && this.right === null && this.color === RB_TREE_COLOR_BLACK;
  }
  _value_less_than(other_node) {
    return this.item.value && other_node.item.value && this.item.value.less_than ? this.item.value.less_than(other_node.item.value) : this.item.value < other_node.item.value;
  }
  less_than(other_node) {
    if (this.item.value === this.item.key && other_node.item.value === other_node.item.key) {
      return this.item.key.less_than(other_node.item.key);
    } else {
      return this.item.key.less_than(other_node.item.key) || this.item.key.equal_to(other_node.item.key) && this._value_less_than(other_node);
    }
  }
  _value_equal(other_node) {
    return this.item.value && other_node.item.value && this.item.value.equal_to ? this.item.value.equal_to(other_node.item.value) : this.item.value === other_node.item.value;
  }
  equal_to(other_node) {
    if (this.item.value === this.item.key && other_node.item.value === other_node.item.key) {
      return this.item.key.equal_to(other_node.item.key);
    } else {
      return this.item.key.equal_to(other_node.item.key) && this._value_equal(other_node);
    }
  }
  intersect(other_node) {
    return this.item.key.intersect(other_node.item.key);
  }
  copy_data(other_node) {
    this.item.key = other_node.item.key;
    this.item.value = other_node.item.value;
  }
  update_max() {
    this.max = this.item.key ? this.item.key.max : void 0;
    if (this.right && this.right.max) {
      const comparable_max = this.item.key.constructor.comparable_max;
      this.max = comparable_max(this.max, this.right.max);
    }
    if (this.left && this.left.max) {
      const comparable_max = this.item.key.constructor.comparable_max;
      this.max = comparable_max(this.max, this.left.max);
    }
  }
  not_intersect_left_subtree(search_node) {
    const comparable_less_than = this.item.key.constructor.comparable_less_than;
    let high = this.left.max.high !== void 0 ? this.left.max.high : this.left.max;
    return comparable_less_than(high, search_node.item.key.low);
  }
  not_intersect_right_subtree(search_node) {
    const comparable_less_than = this.item.key.constructor.comparable_less_than;
    let low = this.right.max.low !== void 0 ? this.right.max.low : this.right.item.key.low;
    return comparable_less_than(search_node.item.key.high, low);
  }
};
var IntervalTree = class {
  constructor() {
    this.root = null;
    this.nil_node = new Node2();
  }
  get size() {
    let count = 0;
    this.tree_walk(this.root, () => count++);
    return count;
  }
  get keys() {
    let res = [];
    this.tree_walk(this.root, (node) => res.push(node.item.key.output ? node.item.key.output() : node.item.key));
    return res;
  }
  get values() {
    let res = [];
    this.tree_walk(this.root, (node) => res.push(node.item.value));
    return res;
  }
  get items() {
    let res = [];
    this.tree_walk(this.root, (node) => res.push({
      key: node.item.key.output ? node.item.key.output() : node.item.key,
      value: node.item.value
    }));
    return res;
  }
  isEmpty() {
    return this.root == null || this.root === this.nil_node;
  }
  clear() {
    this.root = null;
  }
  insert(key, value = key) {
    if (key === void 0)
      return;
    let insert_node = new Node2(key, value, this.nil_node, this.nil_node, null, RB_TREE_COLOR_RED);
    this.tree_insert(insert_node);
    this.recalc_max(insert_node);
    return insert_node;
  }
  exist(key, value = key) {
    let search_node = new Node2(key, value);
    return !!this.tree_search(this.root, search_node);
  }
  remove(key, value = key) {
    let search_node = new Node2(key, value);
    let delete_node = this.tree_search(this.root, search_node);
    if (delete_node) {
      this.tree_delete(delete_node);
    }
    return delete_node;
  }
  search(interval, outputMapperFn = (value, key) => value === key ? key.output() : value) {
    let search_node = new Node2(interval);
    let resp_nodes = [];
    this.tree_search_interval(this.root, search_node, resp_nodes);
    return resp_nodes.map((node) => outputMapperFn(node.item.value, node.item.key));
  }
  intersect_any(interval) {
    let search_node = new Node2(interval);
    return this.tree_find_any_interval(this.root, search_node);
  }
  forEach(visitor) {
    this.tree_walk(this.root, (node) => visitor(node.item.key, node.item.value));
  }
  map(callback) {
    const tree = new IntervalTree();
    this.tree_walk(this.root, (node) => tree.insert(node.item.key, callback(node.item.value, node.item.key)));
    return tree;
  }
  *iterate(interval, outputMapperFn = (value, key) => value === key ? key.output() : value) {
    let node;
    if (interval) {
      node = this.tree_search_nearest_forward(this.root, new Node2(interval));
    } else if (this.root) {
      node = this.local_minimum(this.root);
    }
    while (node) {
      yield outputMapperFn(node.item.value, node.item.key);
      node = this.tree_successor(node);
    }
  }
  recalc_max(node) {
    let node_current = node;
    while (node_current.parent != null) {
      node_current.parent.update_max();
      node_current = node_current.parent;
    }
  }
  tree_insert(insert_node) {
    let current_node = this.root;
    let parent_node = null;
    if (this.root == null || this.root === this.nil_node) {
      this.root = insert_node;
    } else {
      while (current_node !== this.nil_node) {
        parent_node = current_node;
        if (insert_node.less_than(current_node)) {
          current_node = current_node.left;
        } else {
          current_node = current_node.right;
        }
      }
      insert_node.parent = parent_node;
      if (insert_node.less_than(parent_node)) {
        parent_node.left = insert_node;
      } else {
        parent_node.right = insert_node;
      }
    }
    this.insert_fixup(insert_node);
  }
  insert_fixup(insert_node) {
    let current_node;
    let uncle_node;
    current_node = insert_node;
    while (current_node !== this.root && current_node.parent.color === RB_TREE_COLOR_RED) {
      if (current_node.parent === current_node.parent.parent.left) {
        uncle_node = current_node.parent.parent.right;
        if (uncle_node.color === RB_TREE_COLOR_RED) {
          current_node.parent.color = RB_TREE_COLOR_BLACK;
          uncle_node.color = RB_TREE_COLOR_BLACK;
          current_node.parent.parent.color = RB_TREE_COLOR_RED;
          current_node = current_node.parent.parent;
        } else {
          if (current_node === current_node.parent.right) {
            current_node = current_node.parent;
            this.rotate_left(current_node);
          }
          current_node.parent.color = RB_TREE_COLOR_BLACK;
          current_node.parent.parent.color = RB_TREE_COLOR_RED;
          this.rotate_right(current_node.parent.parent);
        }
      } else {
        uncle_node = current_node.parent.parent.left;
        if (uncle_node.color === RB_TREE_COLOR_RED) {
          current_node.parent.color = RB_TREE_COLOR_BLACK;
          uncle_node.color = RB_TREE_COLOR_BLACK;
          current_node.parent.parent.color = RB_TREE_COLOR_RED;
          current_node = current_node.parent.parent;
        } else {
          if (current_node === current_node.parent.left) {
            current_node = current_node.parent;
            this.rotate_right(current_node);
          }
          current_node.parent.color = RB_TREE_COLOR_BLACK;
          current_node.parent.parent.color = RB_TREE_COLOR_RED;
          this.rotate_left(current_node.parent.parent);
        }
      }
    }
    this.root.color = RB_TREE_COLOR_BLACK;
  }
  tree_delete(delete_node) {
    let cut_node;
    let fix_node;
    if (delete_node.left === this.nil_node || delete_node.right === this.nil_node) {
      cut_node = delete_node;
    } else {
      cut_node = this.tree_successor(delete_node);
    }
    if (cut_node.left !== this.nil_node) {
      fix_node = cut_node.left;
    } else {
      fix_node = cut_node.right;
    }
    fix_node.parent = cut_node.parent;
    if (cut_node === this.root) {
      this.root = fix_node;
    } else {
      if (cut_node === cut_node.parent.left) {
        cut_node.parent.left = fix_node;
      } else {
        cut_node.parent.right = fix_node;
      }
      cut_node.parent.update_max();
    }
    this.recalc_max(fix_node);
    if (cut_node !== delete_node) {
      delete_node.copy_data(cut_node);
      delete_node.update_max();
      this.recalc_max(delete_node);
    }
    if (cut_node.color === RB_TREE_COLOR_BLACK) {
      this.delete_fixup(fix_node);
    }
  }
  delete_fixup(fix_node) {
    let current_node = fix_node;
    let brother_node;
    while (current_node !== this.root && current_node.parent != null && current_node.color === RB_TREE_COLOR_BLACK) {
      if (current_node === current_node.parent.left) {
        brother_node = current_node.parent.right;
        if (brother_node.color === RB_TREE_COLOR_RED) {
          brother_node.color = RB_TREE_COLOR_BLACK;
          current_node.parent.color = RB_TREE_COLOR_RED;
          this.rotate_left(current_node.parent);
          brother_node = current_node.parent.right;
        }
        if (brother_node.left.color === RB_TREE_COLOR_BLACK && brother_node.right.color === RB_TREE_COLOR_BLACK) {
          brother_node.color = RB_TREE_COLOR_RED;
          current_node = current_node.parent;
        } else {
          if (brother_node.right.color === RB_TREE_COLOR_BLACK) {
            brother_node.color = RB_TREE_COLOR_RED;
            brother_node.left.color = RB_TREE_COLOR_BLACK;
            this.rotate_right(brother_node);
            brother_node = current_node.parent.right;
          }
          brother_node.color = current_node.parent.color;
          current_node.parent.color = RB_TREE_COLOR_BLACK;
          brother_node.right.color = RB_TREE_COLOR_BLACK;
          this.rotate_left(current_node.parent);
          current_node = this.root;
        }
      } else {
        brother_node = current_node.parent.left;
        if (brother_node.color === RB_TREE_COLOR_RED) {
          brother_node.color = RB_TREE_COLOR_BLACK;
          current_node.parent.color = RB_TREE_COLOR_RED;
          this.rotate_right(current_node.parent);
          brother_node = current_node.parent.left;
        }
        if (brother_node.left.color === RB_TREE_COLOR_BLACK && brother_node.right.color === RB_TREE_COLOR_BLACK) {
          brother_node.color = RB_TREE_COLOR_RED;
          current_node = current_node.parent;
        } else {
          if (brother_node.left.color === RB_TREE_COLOR_BLACK) {
            brother_node.color = RB_TREE_COLOR_RED;
            brother_node.right.color = RB_TREE_COLOR_BLACK;
            this.rotate_left(brother_node);
            brother_node = current_node.parent.left;
          }
          brother_node.color = current_node.parent.color;
          current_node.parent.color = RB_TREE_COLOR_BLACK;
          brother_node.left.color = RB_TREE_COLOR_BLACK;
          this.rotate_right(current_node.parent);
          current_node = this.root;
        }
      }
    }
    current_node.color = RB_TREE_COLOR_BLACK;
  }
  tree_search(node, search_node) {
    if (node == null || node === this.nil_node)
      return void 0;
    if (search_node.equal_to(node)) {
      return node;
    }
    if (search_node.less_than(node)) {
      return this.tree_search(node.left, search_node);
    } else {
      return this.tree_search(node.right, search_node);
    }
  }
  tree_search_nearest_forward(node, search_node) {
    let best;
    let curr = node;
    while (curr && curr !== this.nil_node) {
      if (curr.less_than(search_node)) {
        if (curr.intersect(search_node)) {
          best = curr;
          curr = curr.left;
        } else {
          curr = curr.right;
        }
      } else {
        if (!best || curr.less_than(best))
          best = curr;
        curr = curr.left;
      }
    }
    return best || null;
  }
  tree_search_interval(node, search_node, res) {
    if (node != null && node !== this.nil_node) {
      if (node.left !== this.nil_node && !node.not_intersect_left_subtree(search_node)) {
        this.tree_search_interval(node.left, search_node, res);
      }
      if (node.intersect(search_node)) {
        res.push(node);
      }
      if (node.right !== this.nil_node && !node.not_intersect_right_subtree(search_node)) {
        this.tree_search_interval(node.right, search_node, res);
      }
    }
  }
  tree_find_any_interval(node, search_node) {
    let found = false;
    if (node != null && node !== this.nil_node) {
      if (node.left !== this.nil_node && !node.not_intersect_left_subtree(search_node)) {
        found = this.tree_find_any_interval(node.left, search_node);
      }
      if (!found) {
        found = node.intersect(search_node);
      }
      if (!found && node.right !== this.nil_node && !node.not_intersect_right_subtree(search_node)) {
        found = this.tree_find_any_interval(node.right, search_node);
      }
    }
    return found;
  }
  local_minimum(node) {
    let node_min = node;
    while (node_min.left != null && node_min.left !== this.nil_node) {
      node_min = node_min.left;
    }
    return node_min;
  }
  local_maximum(node) {
    let node_max = node;
    while (node_max.right != null && node_max.right !== this.nil_node) {
      node_max = node_max.right;
    }
    return node_max;
  }
  tree_successor(node) {
    let node_successor;
    let current_node;
    let parent_node;
    if (node.right !== this.nil_node) {
      node_successor = this.local_minimum(node.right);
    } else {
      current_node = node;
      parent_node = node.parent;
      while (parent_node != null && parent_node.right === current_node) {
        current_node = parent_node;
        parent_node = parent_node.parent;
      }
      node_successor = parent_node;
    }
    return node_successor;
  }
  rotate_left(x) {
    let y = x.right;
    x.right = y.left;
    if (y.left !== this.nil_node) {
      y.left.parent = x;
    }
    y.parent = x.parent;
    if (x === this.root) {
      this.root = y;
    } else {
      if (x === x.parent.left) {
        x.parent.left = y;
      } else {
        x.parent.right = y;
      }
    }
    y.left = x;
    x.parent = y;
    if (x !== null && x !== this.nil_node) {
      x.update_max();
    }
    y = x.parent;
    if (y != null && y !== this.nil_node) {
      y.update_max();
    }
  }
  rotate_right(y) {
    let x = y.left;
    y.left = x.right;
    if (x.right !== this.nil_node) {
      x.right.parent = y;
    }
    x.parent = y.parent;
    if (y === this.root) {
      this.root = x;
    } else {
      if (y === y.parent.left) {
        y.parent.left = x;
      } else {
        y.parent.right = x;
      }
    }
    x.right = y;
    y.parent = x;
    if (y !== null && y !== this.nil_node) {
      y.update_max();
    }
    x = y.parent;
    if (x != null && x !== this.nil_node) {
      x.update_max();
    }
  }
  tree_walk(node, action) {
    if (node != null && node !== this.nil_node) {
      this.tree_walk(node.left, action);
      action(node);
      this.tree_walk(node.right, action);
    }
  }
  testRedBlackProperty() {
    let res = true;
    this.tree_walk(this.root, function(node) {
      if (node.color === RB_TREE_COLOR_RED) {
        if (!(node.left.color === RB_TREE_COLOR_BLACK && node.right.color === RB_TREE_COLOR_BLACK)) {
          res = false;
        }
      }
    });
    return res;
  }
  testBlackHeightProperty(node) {
    let height = 0;
    let heightLeft = 0;
    let heightRight = 0;
    if (node.color === RB_TREE_COLOR_BLACK) {
      height++;
    }
    if (node.left !== this.nil_node) {
      heightLeft = this.testBlackHeightProperty(node.left);
    } else {
      heightLeft = 1;
    }
    if (node.right !== this.nil_node) {
      heightRight = this.testBlackHeightProperty(node.right);
    } else {
      heightRight = 1;
    }
    if (heightLeft !== heightRight) {
      throw new Error("Red-black height property violated");
    }
    height += heightLeft;
    return height;
  }
};

// linker/liveLinker.ts
function isDescendant(parent, child, maxDepth = 10) {
  let node = child.parentNode;
  let depth = 0;
  while (node != null && depth < maxDepth) {
    if (node === parent) {
      return true;
    }
    node = node.parentNode;
    depth++;
  }
  return false;
}
var VirtualLinkWidget = class extends import_view.WidgetType {
  constructor(match) {
    super();
    this.match = match;
  }
  toDOM(view) {
    const cmTableWidget = view.dom.closest(".cm-table-widget");
    const tableWrapper = view.dom.closest(".table-cell-wrapper");
    const inTableCellEditor = Boolean(cmTableWidget && tableWrapper);
    const element = this.match.getCompleteLinkElement(inTableCellEditor);
    let inBoldContext = false;
    let inItalicContext = false;
    let inHighlightContext = false;
    let inStrikethroughContext = false;
    let inCommentContext = false;
    let inHeaderContext = false;
    const linkRange = { from: this.match.from, to: this.match.to };
    const expandRange = 10;
    const searchFrom = Math.max(0, this.match.from - expandRange);
    const searchTo = Math.min(view.state.doc.length, this.match.to + expandRange);
    (0, import_language.syntaxTree)(view.state).iterate({
      from: searchFrom,
      to: searchTo,
      enter(node) {
        const type = node.type.name;
        const nodeRange = { from: node.from, to: node.to };
        if (linkRange.from >= nodeRange.from && linkRange.to <= nodeRange.to) {
          if (type.includes("strong")) {
            inBoldContext = true;
          }
          if (type.includes("em")) {
            inItalicContext = true;
          }
          if (type.includes("highlight") || type.includes("mark")) {
            inHighlightContext = true;
          }
          if (type.includes("strikethrough") || type.includes("strike") || type.includes("del")) {
            inStrikethroughContext = true;
          }
          if (type.includes("comment")) {
            inCommentContext = true;
          }
          if (type.includes("header")) {
            inHeaderContext = true;
          }
        }
      }
    });
    this.match.isBoldContext = inBoldContext || this.match.isBoldContext;
    this.match.isItalicContext = inItalicContext;
    this.match.isHighlightContext = inHighlightContext;
    this.match.isStrikethroughContext = inStrikethroughContext;
    this.match.isCommentContext = inCommentContext;
    this.match.isTripleStarContext = inBoldContext && inItalicContext;
    if (this.match.isBoldContext) {
      element.classList.add("cm-strong");
    }
    if (this.match.isItalicContext) {
      element.classList.add("cm-em");
    }
    if (this.match.isHighlightContext) {
      element.classList.add("cm-highlight");
    }
    if (this.match.isStrikethroughContext) {
      element.classList.add("cm-strikethrough");
    }
    if (this.match.isCommentContext) {
      element.classList.add("virtual-link-in-comment");
    }
    if (inHeaderContext) {
      element.classList.add("virtual-link-in-header");
    }
    if (this.match.isTripleStarContext) {
      element.classList.add("cm-strong", "cm-em");
    }
    return element;
  }
  get estimatedHeight() {
    return -1;
  }
};
var AutoLinkerPlugin = class {
  constructor(view, app, settings, updateManager, plugin) {
    this.lastCursorPos = 0;
    this.lastActiveFile = "";
    this.lastViewUpdate = null;
    this.viewUpdateDomToFileMap = /* @__PURE__ */ new Map();
    this.app = app;
    this.plugin = plugin;
    this.settings = settings;
    const { vault } = this.app;
    this.vault = vault;
    this.linkerCache = LinkerCache.getInstance(app, this.settings);
    this.decorations = this.buildDecorations(view);
    updateManager.registerCallback(() => {
      if (this.lastViewUpdate) {
        this.update(this.lastViewUpdate, true);
      }
    });
  }
  update(update, force = false) {
    var _a2;
    const activeView = this.app.workspace.getActiveViewOfType(import_obsidian5.MarkdownView);
    const cmTableWidget = update.view.dom.closest(".cm-table-widget");
    const tableWrapper = update.view.dom.closest(".table-cell-wrapper");
    const inTableCellEditor = Boolean(cmTableWidget && tableWrapper);
    let updateIsOnActiveView = false;
    if (this.settings.fixIMEProblem || this.settings.excludeLinksInCurrentLine || this.settings.excludeLinksToOwnNote) {
      const domFromUpdate = update.view.dom;
      const domFromWorkspace = activeView == null ? void 0 : activeView.contentEl;
      updateIsOnActiveView = domFromWorkspace ? isDescendant(domFromWorkspace, domFromUpdate, 3) : false;
      if (!updateIsOnActiveView && inTableCellEditor) {
        updateIsOnActiveView = true;
      }
      if (updateIsOnActiveView) {
        this.viewUpdateDomToFileMap.set(domFromUpdate, activeView == null ? void 0 : activeView.file);
      }
    }
    const cursorPos = update.view.state.selection.main.from;
    const activeFile = (_a2 = this.app.workspace.getActiveFile()) == null ? void 0 : _a2.path;
    const fileChanged = activeFile != this.lastActiveFile;
    if (force || this.lastCursorPos != cursorPos || update.docChanged || fileChanged || update.viewportChanged) {
      this.lastCursorPos = cursorPos;
      this.linkerCache.updateCache(force);
      this.decorations = this.buildDecorations(update.view, updateIsOnActiveView);
      this.lastActiveFile = activeFile != null ? activeFile : "";
    }
    this.lastViewUpdate = update;
  }
  destroy() {
  }
  getParentElementInfo(element, maxDepth = 5) {
    const parents = [];
    let current = element.parentElement;
    let depth = 0;
    while (current && depth < maxDepth) {
      parents.push({
        tag: current.tagName,
        classes: Array.from(current.classList).join(" ")
      });
      current = current.parentElement;
      depth++;
    }
    return parents;
  }
  findTableCellLineBoundary(view, cursorPos, findStart) {
    const doc = view.state.doc;
    if (findStart) {
      for (let pos = cursorPos; pos >= 0; pos--) {
        if (pos === 0) {
          return 0;
        }
        const char = doc.sliceString(pos - 1, pos);
        if (char === "\n") {
          return pos;
        }
      }
      return 0;
    } else {
      for (let pos = cursorPos; pos <= doc.length; pos++) {
        if (pos === doc.length) {
          return doc.length;
        }
        const char = doc.sliceString(pos, pos + 1);
        if (char === "\n") {
          return pos;
        }
      }
      return doc.length;
    }
  }
  buildDecorations(view, viewIsActive = true) {
    var _a2, _b2, _c, _d;
    const builder = new import_state.RangeSetBuilder();
    if (!this.settings.linkerActivated) {
      return builder.finish();
    }
    const dom = view.dom;
    const mappedFile = this.viewUpdateDomToFileMap.get(dom);
    const excludedFolders = this.settings.excludedDirectoriesForLinking;
    if (excludedFolders.length > 0) {
      const path = (_d = (_a2 = mappedFile == null ? void 0 : mappedFile.parent) == null ? void 0 : _a2.path) != null ? _d : (_c = (_b2 = this.app.workspace.getActiveFile()) == null ? void 0 : _b2.parent) == null ? void 0 : _c.path;
      if (excludedFolders.includes(path != null ? path : ""))
        return builder.finish();
    }
    const explicitlyLinkedFiles = /* @__PURE__ */ new Set();
    const alreadyLinkedFiles = /* @__PURE__ */ new Set();
    for (let { from, to } of view.visibleRanges) {
      this.linkerCache.reset();
      const text = view.state.doc.sliceString(from, to);
      let matches = [];
      let id = 0;
      for (let i = 0; i <= text.length; i) {
        const codePoint = text.codePointAt(i);
        const char = i < text.length ? String.fromCodePoint(codePoint) : "\n";
        const isWordBoundary = PrefixTree.checkWordBoundary(char);
        if (this.settings.matchAnyPartsOfWords || this.settings.matchBeginningOfWords || isWordBoundary) {
          const currentNodes = this.linkerCache.cache.getCurrentMatchNodes(i, this.settings.excludeLinksToOwnNote ? mappedFile : null);
          if (currentNodes.length > 0) {
            for (const node of currentNodes) {
              if (!this.settings.matchAnyPartsOfWords) {
                if (this.settings.matchBeginningOfWords && !node.startsAtWordBoundary && (this.settings.matchEndOfWords && !isWordBoundary)) {
                  continue;
                }
              }
              const nFrom = node.start;
              const nTo = node.end;
              const name = text.slice(nFrom, nTo);
              const isAlias = node.isAlias;
              const aFrom = from + nFrom;
              const aTo = from + nTo;
              const filteredFiles = Array.from(node.files).filter((file) => {
                return !this.settings.excludedExtensions.some((ext) => file.path.toLowerCase().endsWith(ext.toLowerCase()));
              });
              const nameToCheck = this.settings.matchCaseSensitive ? name : name.toLowerCase();
              const excludedKeywords = this.settings.matchCaseSensitive ? this.settings.excludedKeywords : this.settings.excludedKeywords.map((k) => k.toLowerCase());
              if (filteredFiles.length > 0 && !excludedKeywords.includes(nameToCheck)) {
                const virtualMatch = new VirtualMatch(id++, name, aFrom, aTo, filteredFiles, isAlias ? 1 /* Alias */ : 0 /* Note */, !isWordBoundary, this.settings, this.plugin, node.headerId);
                if (filteredFiles.length > 1) {
                  filteredFiles.forEach((file, index) => {
                    if (index === 0)
                      return;
                    const fileNodes = this.linkerCache.cache.getCurrentMatchNodes(i, null, file);
                    if (fileNodes && fileNodes.length > 0 && fileNodes[0].headerId) {
                      virtualMatch.setFileHeaderId(file, fileNodes[0].headerId);
                    }
                  });
                }
                matches.push(virtualMatch);
              }
            }
          }
        }
        this.linkerCache.cache.pushChar(char);
        i += char.length;
      }
      matches = VirtualMatch.sort(matches);
      const excludedIntervalTree = new IntervalTree();
      let excludedTypes = ["codeblock", "code-block", "inline-code", "internal-link", "link", "url", "hashtag"];
      if (!this.settings.allowLinksInHeaders) {
        excludedTypes.push("header-");
      }
      const app = this.app;
      (0, import_language.syntaxTree)(view.state).iterate({
        from,
        to,
        enter(node) {
          const type = node.type.name;
          const types = type.split("_");
          for (const excludedType of excludedTypes) {
            if (type.contains(excludedType)) {
              excludedIntervalTree.insert([node.from, node.to]);
              const isLinkIfHavingTypes = [["string", "url"], "hmd-internal-link", "internal-link"];
              isLinkIfHavingTypes.forEach((t2) => {
                var _a3;
                const tList = Array.isArray(t2) ? t2 : [t2];
                if (tList.every((tt) => types.includes(tt))) {
                  const text2 = view.state.doc.sliceString(node.from, node.to);
                  const linkedFile = app.metadataCache.getFirstLinkpathDest(text2, (_a3 = mappedFile == null ? void 0 : mappedFile.path) != null ? _a3 : "");
                  if (linkedFile) {
                    explicitlyLinkedFiles.add(linkedFile);
                  }
                }
              });
            }
          }
        }
      });
      if (this.settings.excludeLinksToRealLinkedFiles) {
        matches = VirtualMatch.filterAlreadyLinked(matches, explicitlyLinkedFiles);
      }
      if (this.settings.onlyLinkOnce) {
        matches = VirtualMatch.filterAlreadyLinked(matches, alreadyLinkedFiles);
      }
      matches = VirtualMatch.filterOverlapping(matches, this.settings.onlyLinkOnce, excludedIntervalTree);
      matches.forEach((addition) => addition.files.forEach((f) => alreadyLinkedFiles.add(f)));
      const cursorPos = view.state.selection.main.from;
      const excludeLine = viewIsActive && this.settings.excludeLinksInCurrentLine;
      const fixIMEProblem = viewIsActive && this.settings.fixIMEProblem;
      let needImeFix = false;
      let inTableCellEditor = false;
      const tableIndicators = [
        ".cm-table-widget",
        ".table-cell-wrapper",
        ".cm-table",
        ".cm-table-cell",
        '[class*="table"]',
        '[class*="cell"]',
        ".markdown-table",
        ".markdown-table-cell"
      ];
      for (const indicator of tableIndicators) {
        if (view.dom.closest(indicator)) {
          inTableCellEditor = true;
          break;
        }
      }
      if (!inTableCellEditor) {
        let parent = view.dom.parentElement;
        while (parent && parent !== activeDocument.body) {
          const parentClasses = Array.from(parent.classList);
          if (parentClasses.some((cls) => cls.includes("table") || cls.includes("cell"))) {
            inTableCellEditor = true;
            break;
          }
          parent = parent.parentElement;
        }
      }
      let lineStart, lineEnd;
      if (inTableCellEditor) {
        lineStart = this.findTableCellLineBoundary(view, cursorPos, true);
        lineEnd = this.findTableCellLineBoundary(view, cursorPos, false);
      } else {
        const line = view.state.doc.lineAt(cursorPos);
        lineStart = line.from;
        lineEnd = line.to;
      }
      matches.forEach((addition) => {
        const [from2, to2] = [addition.from, addition.to];
        const cursorNearby = cursorPos >= from2 - 0 && cursorPos <= to2 + 0;
        const additionIsInCurrentLine = from2 >= lineStart && to2 <= lineEnd;
        let additionIsInComment = false;
        if (additionIsInCurrentLine) {
          (0, import_language.syntaxTree)(view.state).iterate({
            from: from2,
            to: to2,
            enter(node) {
              if (node.name.contains("comment")) {
                additionIsInComment = true;
              }
            }
          });
        }
        if (fixIMEProblem) {
          needImeFix = true;
          if (additionIsInCurrentLine && cursorPos > to2) {
            let gapString = view.state.sliceDoc(to2, cursorPos);
            let strBeforeAdd = view.state.sliceDoc(lineStart, from2);
            const regAddInLineStart = /(^\s*$)|(^\s*- +$)|(^\s*#{1,6} $)|(^\s*>+ *$)|(^\s*- +#{1,6} +$)|(^\s*> \[![\w-]+\][+-]? +$)/;
            if (!regAddInLineStart.test(strBeforeAdd)) {
              needImeFix = false;
            } else {
              const regStrMayIMEon = /^[a-zA-Z]+[a-zA-Z' ]*[a-zA-Z]$|^[a-zA-Z]$/;
              if (!regStrMayIMEon.test(gapString) || /[' ]{2}/.test(gapString)) {
                needImeFix = false;
              }
            }
          } else {
            needImeFix = false;
          }
        }
        if (!cursorNearby && !needImeFix && !(excludeLine && additionIsInCurrentLine && !additionIsInComment)) {
          builder.add(from2, to2, import_view.Decoration.replace({
            widget: new VirtualLinkWidget(addition)
          }));
        }
      });
    }
    return builder.finish();
  }
};
var pluginSpec = {
  decorations: (value) => value.decorations
};
var liveLinkerPlugin = (app, settings, updateManager, plugin) => {
  return import_view.ViewPlugin.define((editorView) => {
    return new AutoLinkerPlugin(editorView, app, settings, updateManager, plugin);
  }, pluginSpec);
};

// main.ts
function dirname(filePath) {
  const lastSlashIndex = filePath.lastIndexOf("/");
  return lastSlashIndex === -1 ? "" : filePath.substring(0, lastSlashIndex);
}
function basename(filePath) {
  const lastSlashIndex = filePath.lastIndexOf("/");
  return lastSlashIndex === -1 ? filePath : filePath.substring(lastSlashIndex + 1);
}
function relative(from, to) {
  if (from === to)
    return "";
  const fromParts = from.split("/").filter((part) => part !== "");
  const toParts = to.split("/").filter((part) => part !== "");
  let commonLength = 0;
  while (commonLength < fromParts.length && commonLength < toParts.length && fromParts[commonLength] === toParts[commonLength]) {
    commonLength++;
  }
  const upLevels = fromParts.length - commonLength;
  const downParts = toParts.slice(commonLength);
  const upPath = upLevels > 0 ? "../".repeat(upLevels) : "./";
  const downPath = downParts.join("/");
  return downPath ? upPath + downPath : upPath.slice(0, -1);
}
function handleTableCellConversion(targetElement, app, settings, updateManager) {
  var _a2;
  const from = parseInt(targetElement.getAttribute("from") || "-1");
  const to = parseInt(targetElement.getAttribute("to") || "-1");
  const text = targetElement.getAttribute("origin-text") || "";
  const headerId = targetElement.getAttribute("data-heading-id");
  if (from === -1 || to === -1) {
    return;
  }
  const activeFile = app.workspace.getActiveFile();
  if (!activeFile) {
    return;
  }
  const href = targetElement.getAttribute("href");
  if (!href) {
    return;
  }
  let targetPath = href;
  let finalHeaderId = headerId;
  if (href.includes("#")) {
    const parts = href.split("#");
    targetPath = parts[0];
    finalHeaderId = parts[1] || headerId;
  }
  const activeFilePath = activeFile.path;
  const targetFile = app.metadataCache.getFirstLinkpathDest(targetPath, activeFilePath);
  if (!targetFile) {
    return;
  }
  const linkPath = app.metadataCache.fileToLinktext(targetFile, activeFilePath);
  const finalPath = finalHeaderId ? `${linkPath}#${finalHeaderId}` : linkPath;
  const useMarkdownLinks = settings.useDefaultLinkStyleForConversion ? settings.defaultUseMarkdownLinks : settings.useMarkdownLinks;
  let replacement = "";
  if (useMarkdownLinks) {
    const escapedText = text.replace(/[\\|]/g, "\\$&");
    replacement = `[${escapedText}](${finalPath})`;
  } else {
    const escapedText = text.replace(/[\\|]/g, "\\$&");
    replacement = `[[${finalPath}\\|${escapedText}]]`;
  }
  const editor = (_a2 = app.workspace.getActiveViewOfType(import_obsidian6.MarkdownView)) == null ? void 0 : _a2.editor;
  if (editor) {
    let fromPos = editor.offsetToPos(from);
    let toPos = editor.offsetToPos(to);
    if (fromPos && toPos) {
      const tableCellElement = targetElement.closest("td, th");
      if (tableCellElement) {
        const cellText = tableCellElement.textContent || "";
        const originText = targetElement.getAttribute("origin-text") || "";
        let textIndex = cellText.indexOf(originText);
        if (textIndex === -1) {
          const escapedOriginText = originText.replace(/[\\|]/g, "\\$&");
          textIndex = cellText.indexOf(escapedOriginText);
        }
        if (textIndex !== -1) {
          const docText = editor.getValue();
          const lines = docText.split("\n");
          let targetLine = -1;
          let preciseOffset = -1;
          const tableRowElement = tableCellElement.closest("tr");
          if (tableRowElement) {
            const cellIndex = Array.from(tableRowElement.children).indexOf(tableCellElement);
            const tableElement = tableRowElement.closest("table");
            let domRowIndex = -1;
            if (tableElement) {
              const allRows = tableElement.querySelectorAll("tr");
              allRows.forEach((row, idx) => {
                if (row === tableRowElement) {
                  domRowIndex = idx;
                }
              });
            }
            const isSeparatorRow = (rowLine) => {
              const trimmed = rowLine.trim();
              return /^\|[\s\-:]+\|$/.test(trimmed) || /^\|[\s\-:|]+\|$/.test(trimmed);
            };
            const splitTableRow = (rowLine) => {
              const cells = [];
              let currentCell = "";
              let inLink = false;
              for (let i = 0; i < rowLine.length; i++) {
                const char = rowLine[i];
                const nextChar = rowLine[i + 1];
                if (char === "[" && nextChar === "[") {
                  inLink = true;
                  currentCell += char;
                } else if (char === "]" && nextChar === "]" && inLink) {
                  inLink = false;
                  currentCell += char;
                } else if (char === "|" && !inLink) {
                  cells.push(currentCell);
                  currentCell = "";
                } else {
                  currentCell += char;
                }
              }
              cells.push(currentCell);
              return cells;
            };
            const nonSeparatorRows = [];
            let domRowCounter = 0;
            for (let i = 0; i < lines.length; i++) {
              const line = lines[i];
              if (!line.trim().startsWith("|"))
                continue;
              if (isSeparatorRow(line))
                continue;
              nonSeparatorRows.push({
                docLineIndex: i,
                domRowIndex: domRowCounter
              });
              domRowCounter++;
            }
            let targetDocLine = -1;
            for (const row of nonSeparatorRows) {
              if (row.domRowIndex === domRowIndex) {
                targetDocLine = row.docLineIndex;
                break;
              }
            }
            if (targetDocLine >= 0 && targetDocLine < lines.length) {
              const line = lines[targetDocLine];
              const cells = splitTableRow(line);
              const mdCellIndex = cellIndex + 1;
              if (mdCellIndex < cells.length) {
                const cellContent = cells[mdCellIndex].trim();
                const cellTextIndex = cellContent.indexOf(originText);
                if (cellTextIndex !== -1) {
                  let offset = 0;
                  let pipeCount = 0;
                  for (let c = 0; c < line.length; c++) {
                    const char = line[c];
                    const isInWikiLink = () => {
                      let depth = 0;
                      for (let j = c - 1; j >= 0; j--) {
                        if (line[j] === "]" && line[j - 1] === "]") {
                          depth++;
                          j--;
                        } else if (line[j] === "[" && line[j - 1] === "[") {
                          depth--;
                          j--;
                          if (depth < 0)
                            return true;
                        }
                      }
                      return false;
                    };
                    if (char === "|" && !isInWikiLink()) {
                      pipeCount++;
                      if (pipeCount === mdCellIndex) {
                        offset = c + 1;
                        while (offset < line.length && line[offset] === " ") {
                          offset++;
                        }
                        break;
                      }
                    }
                  }
                  targetLine = targetDocLine;
                  preciseOffset = offset + cellTextIndex;
                }
              }
            }
          }
          if (targetLine === -1 || preciseOffset === -1) {
            for (let i = 0; i < lines.length; i++) {
              const line = lines[i];
              if (line.includes("|") && line.includes(originText)) {
                const lineTextIndex = line.indexOf(originText);
                if (lineTextIndex !== -1) {
                  targetLine = i;
                  preciseOffset = lineTextIndex;
                  break;
                }
              }
            }
          }
          if (targetLine !== -1 && preciseOffset !== -1) {
            fromPos = { line: targetLine, ch: preciseOffset };
            toPos = { line: targetLine, ch: preciseOffset + originText.length };
          }
        }
      }
      const currentLineText = editor.getLine(fromPos.line);
      const originalTextAtPosition = currentLineText.substring(fromPos.ch, toPos.ch);
      const expectedText = targetElement.getAttribute("origin-text") || "";
      if (originalTextAtPosition === expectedText) {
        editor.replaceRange(replacement, fromPos, toPos);
        updateManager.update();
        window.setTimeout(() => {
          editor.getLine(fromPos.line);
        }, 100);
      } else {
        const tableCellElement2 = targetElement.closest("td, th");
        if (tableCellElement2) {
          const cellText = tableCellElement2.textContent || "";
          let textIndex = cellText.indexOf(expectedText);
          if (textIndex === -1) {
            const escapedExpectedText = expectedText.replace(/[\\|]/g, "\\$&");
            textIndex = cellText.indexOf(escapedExpectedText);
          }
          if (textIndex !== -1) {
            const docText = editor.getValue();
            const lines = docText.split("\n");
            let targetLine = -1;
            let preciseOffset = -1;
            const tableRowElement = tableCellElement2.closest("tr");
            if (tableRowElement) {
              const cellIndex = Array.from(tableRowElement.children).indexOf(tableCellElement2);
              const tableElement = tableRowElement.closest("table");
              let domRowIndex = -1;
              if (tableElement) {
                const allRows = tableElement.querySelectorAll("tr");
                allRows.forEach((row, idx) => {
                  if (row === tableRowElement) {
                    domRowIndex = idx;
                  }
                });
              }
              const isSeparatorRow = (rowLine) => {
                const trimmed = rowLine.trim();
                return /^\|[\s\-:]+\|$/.test(trimmed) || /^\|[\s\-:|]+\|$/.test(trimmed);
              };
              const splitTableRow = (rowLine) => {
                const cells = [];
                let currentCell = "";
                let inLink = false;
                for (let k = 0; k < rowLine.length; k++) {
                  const char = rowLine[k];
                  const nextChar = rowLine[k + 1];
                  if (char === "[" && nextChar === "[") {
                    inLink = true;
                    currentCell += char;
                  } else if (char === "]" && nextChar === "]" && inLink) {
                    inLink = false;
                    currentCell += char;
                  } else if (char === "|" && !inLink) {
                    cells.push(currentCell);
                    currentCell = "";
                  } else {
                    currentCell += char;
                  }
                }
                cells.push(currentCell);
                return cells;
              };
              const nonSeparatorRows = [];
              let domRowCounter = 0;
              for (let i = 0; i < lines.length; i++) {
                const line = lines[i];
                if (!line.trim().startsWith("|"))
                  continue;
                if (isSeparatorRow(line))
                  continue;
                nonSeparatorRows.push({
                  docLineIndex: i,
                  domRowIndex: domRowCounter
                });
                domRowCounter++;
              }
              let targetDocLine = -1;
              for (const row of nonSeparatorRows) {
                if (row.domRowIndex === domRowIndex) {
                  targetDocLine = row.docLineIndex;
                  break;
                }
              }
              if (targetDocLine >= 0 && targetDocLine < lines.length) {
                const line = lines[targetDocLine];
                const cells = splitTableRow(line);
                const mdCellIndex = cellIndex + 1;
                if (mdCellIndex < cells.length) {
                  const cellContent = cells[mdCellIndex].trim();
                  const cellTextIndex = cellContent.indexOf(expectedText);
                  if (cellTextIndex !== -1) {
                    let offset = 0;
                    let pipeCount = 0;
                    for (let c = 0; c < line.length; c++) {
                      const char = line[c];
                      const isInWikiLink = () => {
                        let depth = 0;
                        for (let j = c - 1; j >= 0; j--) {
                          if (line[j] === "]" && line[j - 1] === "]") {
                            depth++;
                            j--;
                          } else if (line[j] === "[" && line[j - 1] === "[") {
                            depth--;
                            j--;
                            if (depth < 0)
                              return true;
                          }
                        }
                        return false;
                      };
                      if (char === "|" && !isInWikiLink()) {
                        pipeCount++;
                        if (pipeCount === mdCellIndex) {
                          offset = c + 1;
                          while (offset < line.length && line[offset] === " ") {
                            offset++;
                          }
                          break;
                        }
                      }
                    }
                    targetLine = targetDocLine;
                    preciseOffset = offset + cellTextIndex;
                  }
                }
              }
            }
            if (targetLine === -1 || preciseOffset === -1) {
              for (let i = 0; i < lines.length; i++) {
                const line = lines[i];
                if (line.includes("|") && line.includes(expectedText)) {
                  const lineTextIndex = line.indexOf(expectedText);
                  if (lineTextIndex !== -1) {
                    targetLine = i;
                    preciseOffset = lineTextIndex;
                    break;
                  }
                }
              }
            }
            if (targetLine !== -1 && preciseOffset !== -1) {
              fromPos = { line: targetLine, ch: preciseOffset };
              toPos = { line: targetLine, ch: preciseOffset + expectedText.length };
              editor.replaceRange(replacement, fromPos, toPos);
              updateManager.update();
              return;
            }
          }
        }
      }
    }
  }
}
var DEFAULT_SETTINGS = {
  autoToggleByMode: false,
  advancedSettings: true,
  linkerActivated: true,
  matchAnyPartsOfWords: true,
  matchEndOfWords: true,
  matchBeginningOfWords: true,
  suppressSuffixForSubWords: false,
  includeAllFiles: true,
  linkerDirectories: ["Glossary"],
  excludedDirectories: [],
  excludedDirectoriesForLinking: [],
  virtualLinkSuffix: "",
  virtualLinkAliasSuffix: "",
  excludedExtensions: [".mp4"],
  useMarkdownLinks: false,
  linkFormat: "shortest",
  defaultUseMarkdownLinks: false,
  defaultLinkFormat: "shortest",
  useDefaultLinkStyleForConversion: true,
  applyDefaultLinkStyling: true,
  alternativeDisplayStyle: true,
  includeHeaders: true,
  headerMatchSymbols: true,
  headerMatchOnlyBetweenSymbols: false,
  headerMatchStartSymbol: "\u27E6",
  headerMatchEndSymbol: "\u27E7",
  matchCaseSensitive: false,
  capitalLetterProportionForAutomaticMatchCase: 0.75,
  tagToIgnoreCase: "linker-ignore-case",
  tagToMatchCase: "linker-match-case",
  propertyNameToMatchCase: "linker-match-case",
  propertyNameToIgnoreCase: "linker-ignore-case",
  tagToExcludeFile: "linker-exclude",
  tagToIncludeFile: "linker-include",
  excludeLinksToOwnNote: false,
  fixIMEProblem: true,
  excludeLinksInCurrentLine: true,
  onlyLinkOnce: false,
  excludeLinksToRealLinkedFiles: false,
  includeAliases: true,
  maxReferenceCount: 5,
  maxReferencesToHideLink: 10,
  alwaysShowMultipleReferences: false,
  excludedKeywords: [],
  headerAutoAppendSuffix: true,
  headerAutoAppendSymbol: "\u2631",
  allowLinksInHeaders: true,
  colorOnlyDisplay: true,
  virtualLinkColor: "#517ea0"
};
var LinkerPlugin = class extends import_obsidian6.Plugin {
  constructor() {
    super(...arguments);
    this.updateManager = new ExternalUpdateManager();
  }
  isInCanvas() {
    const activeView = this.app.workspace.getActiveViewOfType(import_obsidian6.MarkdownView);
    if (activeView && activeView.getViewType() === "canvas") {
      return true;
    }
    return false;
  }
  async handleLayoutChange() {
    if (!this.settings.autoToggleByMode)
      return;
    if (this.isInCanvas()) {
      if (!this.settings.linkerActivated) {
        await this.updateSettings({ linkerActivated: true });
      }
      return;
    }
    const activeView = this.app.workspace.getActiveViewOfType(import_obsidian6.MarkdownView);
    if (!activeView)
      return;
    const isPreviewMode = activeView.getMode() === "preview";
    const isEditorMode = activeView.getMode() === "source";
    if (isPreviewMode && this.settings.linkerActivated) {
      await this.updateSettings({ linkerActivated: false });
    } else if (isEditorMode && !this.settings.linkerActivated) {
      await this.updateSettings({ linkerActivated: true });
    }
  }
  async onload() {
    await this.loadSettings();
    if (this.settings.alternativeDisplayStyle) {
      activeWindow.document.body.classList.add("virtual-linker-alt-style");
    }
    if (this.settings.colorOnlyDisplay) {
      activeWindow.document.body.classList.add("virtual-link-color-only");
      activeWindow.document.body.style.setProperty("--virtual-link-color", this.settings.virtualLinkColor);
    }
    this.registerEvent(this.app.workspace.on("layout-change", () => {
      void this.handleLayoutChange();
    }));
    this.registerEvent(this.app.workspace.on("active-leaf-change", () => {
      void this.handleLayoutChange();
    }));
    this.updateManager.registerCallback(() => {
      LinkerCache.getInstance(this.app, this.settings).clearCache();
    });
    this.registerMarkdownPostProcessor((element, context) => {
      context.addChild(new GlossaryLinker(this.app, this.settings, context, element, this));
    });
    this.registerEditorExtension(liveLinkerPlugin(this.app, this.settings, this.updateManager, this));
    this.registerEditorExtension(import_view2.EditorView.updateListener.of((update) => {
      if (!this.settings.alternativeDisplayStyle || !update.docChanged)
        return;
      let minFrom = Infinity;
      let maxTo = -Infinity;
      update.changes.iterChanges((_fromA, _toA, fromB, toB) => {
        if (fromB < minFrom)
          minFrom = fromB;
        if (toB > maxTo)
          maxTo = toB;
      });
      if (minFrom === Infinity)
        return;
      const doc = update.state.doc;
      const startLine = doc.lineAt(minFrom);
      const endLine = doc.lineAt(maxTo - 1 > 0 ? maxTo - 1 : maxTo);
      const changes = [];
      for (let i = startLine.number; i <= endLine.number; i++) {
        const line = doc.line(i);
        let text = line.text;
        if (!text.includes("%%") || !/\S/.test(text))
          continue;
        let searchFrom = 0;
        while (searchFrom < text.length) {
          const startIdx = text.indexOf("%%", searchFrom);
          if (startIdx === -1)
            break;
          const contentStart = startIdx + 2;
          const endIdx = text.indexOf("%%", contentStart);
          if (endIdx === -1) {
            searchFrom = contentStart;
            continue;
          }
          const inner = text.slice(contentStart, endIdx);
          const trimmed = inner.trim();
          if (trimmed !== inner) {
            const fullFrom = line.from + startIdx;
            const fullTo = line.from + endIdx + 2;
            changes.push({
              from: fullFrom,
              to: fullTo,
              insert: `%%${trimmed}%%`
            });
            const before = text.slice(0, startIdx);
            const after = text.slice(endIdx + 2);
            text = before + `%%${trimmed}%%` + after;
            searchFrom = startIdx + trimmed.length + 4;
          } else {
            searchFrom = endIdx + 2;
          }
        }
      }
      if (changes.length > 0) {
        if (changes.length === 1) {
          const ch = changes[0];
          const anchor = ch.from + 2;
          const head = ch.from + ch.insert.length - 2;
          update.view.dispatch({
            changes,
            selection: import_state2.EditorSelection.single(anchor, head)
          });
        } else {
          update.view.dispatch({ changes });
        }
      }
    }));
    this.registerEditorExtension(import_view2.EditorView.updateListener.of((update) => {
      if (!this.settings.headerAutoAppendSuffix || !update.docChanged)
        return;
      const symbol = this.settings.headerAutoAppendSymbol;
      if (!symbol)
        return;
      const doc = update.state.doc;
      let minFrom = Infinity, maxTo = -Infinity;
      update.changes.iterChanges((_a2, _b2, fromB, toB) => {
        if (fromB < minFrom)
          minFrom = fromB;
        if (toB > maxTo)
          maxTo = toB;
      });
      if (minFrom === Infinity)
        return;
      const startLine = doc.lineAt(minFrom);
      const endLine = doc.lineAt(Math.max(0, maxTo - 1));
      const changes = [];
      for (let i = startLine.number; i <= endLine.number; i++) {
        const line = doc.line(i);
        const text = line.text;
        const match = text.match(/^(#{1,6}\s+)(\S.*)$/);
        if (!match)
          continue;
        const prefix = match[1];
        const content = match[2];
        if (content.startsWith(symbol))
          continue;
        changes.push({
          from: line.from + prefix.length,
          to: line.from + prefix.length,
          insert: symbol
        });
      }
      if (changes.length > 0) {
        update.view.dispatch({ changes });
      }
    }));
    this.addSettingTab(new LinkerSettingTab(this.app, this));
    this.registerEvent(this.app.workspace.on("file-menu", (menu, file, source) => this.addContextMenuItem(menu, file, source)));
    this.addCommand({
      id: "toggle-virtual-linker",
      name: "Toggle virtual linker",
      callback: () => {
        void this.updateSettings({ linkerActivated: !this.settings.linkerActivated });
        this.updateManager.update();
      }
    });
    this.addCommand({
      id: "toggle-header-marker",
      name: "Toggle header marker symbol",
      callback: () => {
        void this.updateSettings({ headerAutoAppendSuffix: !this.settings.headerAutoAppendSuffix });
      }
    });
    this.addCommand({
      id: "convert-selected-virtual-links",
      name: "Convert all virtual links in selection to real links",
      checkCallback: (checking) => {
        var _a2;
        const view = this.app.workspace.getActiveViewOfType(import_obsidian6.MarkdownView);
        const editor = view == null ? void 0 : view.editor;
        if (!editor || !editor.somethingSelected()) {
          return false;
        }
        if (checking)
          return true;
        const from = editor.getCursor("from");
        const to = editor.getCursor("to");
        const cmEditor = editor.cm;
        if (!cmEditor)
          return false;
        const selectionRange = cmEditor.dom.querySelector(".cm-content");
        if (!selectionRange)
          return false;
        const virtualLinkElements = Array.from(selectionRange.querySelectorAll("a"));
        const virtualLinks = virtualLinkElements.filter((link) => {
          if (!link.instanceOf(HTMLAnchorElement))
            return false;
          return link.classList.contains("virtual-linker-link") || link.classList.contains("virtual-link-a");
        }).map((link) => ({
          element: link,
          from: parseInt(link.getAttribute("from") || "-1"),
          to: parseInt(link.getAttribute("to") || "-1"),
          text: link.getAttribute("origin-text") || "",
          href: link.getAttribute("href") || "",
          headerId: link.getAttribute("data-heading-id") || ""
        })).filter((link) => {
          const linkFrom = editor.offsetToPos(link.from);
          const linkTo = editor.offsetToPos(link.to);
          return this.isPosWithinRange(linkFrom, linkTo, from, to);
        }).sort((a, b) => a.from - b.from);
        if (virtualLinks.length === 0)
          return false;
        const replacements = [];
        for (const link of virtualLinks) {
          const hrefWithoutAnchor = link.href.split("#")[0];
          const targetFile = this.app.vault.getAbstractFileByPath(hrefWithoutAnchor);
          if (!(targetFile instanceof import_obsidian6.TFile)) {
            continue;
          }
          const activeFile = this.app.workspace.getActiveFile();
          const activeFilePath = (_a2 = activeFile == null ? void 0 : activeFile.path) != null ? _a2 : "";
          let absolutePath = targetFile.path;
          let relativePath = relative(dirname(activeFilePath), dirname(absolutePath)) + "/" + basename(absolutePath);
          relativePath = relativePath.replace(/\\/g, "/");
          const replacementPath = this.app.metadataCache.fileToLinktext(targetFile, activeFilePath);
          const lastPart = replacementPath.split("/").pop();
          if (!lastPart) {
            continue;
          }
          const shortestFile = this.app.metadataCache.getFirstLinkpathDest(lastPart, "");
          let shortestPath = (shortestFile == null ? void 0 : shortestFile.path) === targetFile.path ? lastPart : absolutePath;
          const headerId = link.element.getAttribute("data-heading-id");
          const pathSuffix = headerId ? `#${headerId}` : "";
          if (!replacementPath.endsWith(".md")) {
            if (absolutePath.endsWith(".md"))
              absolutePath = absolutePath.slice(0, -3);
            if (shortestPath.endsWith(".md"))
              shortestPath = shortestPath.slice(0, -3);
            if (relativePath.endsWith(".md"))
              relativePath = relativePath.slice(0, -3);
            absolutePath += pathSuffix;
            shortestPath += pathSuffix;
            relativePath += pathSuffix;
          }
          const useMarkdownLinks = this.settings.useDefaultLinkStyleForConversion ? this.settings.defaultUseMarkdownLinks : this.settings.useMarkdownLinks;
          const linkFormat = this.settings.useDefaultLinkStyleForConversion ? this.settings.defaultLinkFormat : this.settings.linkFormat;
          let replacement = "";
          if (replacementPath === link.text && linkFormat === "shortest") {
            replacement = `[[${replacementPath}]]`;
          } else {
            const path = linkFormat === "shortest" ? shortestPath : linkFormat === "relative" ? relativePath : absolutePath;
            if (useMarkdownLinks) {
              replacement = `[${link.text}](${path})`;
            } else {
              const isInTable = this.isInTableEnvironment(editor, link.from, link.to);
              if (isInTable) {
                const escapedText = link.text.replace(/[\\|]/g, "\\$&");
                replacement = `[[${path}\\|${escapedText}]]`;
              } else {
                replacement = `[[${path}|${link.text}]]`;
              }
            }
          }
          replacements.push({
            from: link.from,
            to: link.to,
            text: replacement
          });
        }
        for (const replacement of replacements.reverse()) {
          const fromPos = editor.offsetToPos(replacement.from);
          const toPos = editor.offsetToPos(replacement.to);
          try {
            editor.replaceRange(replacement.text, fromPos, toPos);
            window.setTimeout(() => {
            }, 100);
            if (this.isInTableEnvironment(editor, replacement.from, replacement.to)) {
              const afterReplacement = editor.getRange(fromPos, editor.offsetToPos(replacement.from + replacement.text.length));
              if (replacement.text !== afterReplacement) {
                editor.replaceRange("", fromPos, toPos);
                for (let i = 0; i < replacement.text.length; i++) {
                  const insertPos = editor.offsetToPos(replacement.from + i);
                  editor.replaceRange(replacement.text[i], insertPos, insertPos);
                }
              }
            }
          } catch (e) {
          }
        }
        return true;
      }
    });
  }
  isInTableEnvironment(editor, _fromOffset, _toOffset) {
    try {
      const fromPos = editor.offsetToPos(_fromOffset);
      const line = editor.getLine(fromPos.line);
      const isTableLine = line.trim().startsWith("|") || line.includes("|");
      if (isTableLine) {
        return true;
      }
      const contextLines = 3;
      for (let i = Math.max(0, fromPos.line - contextLines); i <= Math.min(editor.lineCount() - 1, fromPos.line + contextLines); i++) {
        const contextLine = editor.getLine(i);
        if (contextLine.trim().startsWith("|") || contextLine.includes("|")) {
          return true;
        }
      }
      return false;
    } catch (e) {
      return false;
    }
  }
  isPosWithinRange(linkFrom, linkTo, selectionFrom, selectionTo) {
    return (linkFrom.line > selectionFrom.line || linkFrom.line === selectionFrom.line && linkFrom.ch >= selectionFrom.ch) && (linkTo.line < selectionTo.line || linkTo.line === selectionTo.line && linkTo.ch <= selectionTo.ch);
  }
  addContextMenuItem(menu, file, _source) {
    if (!file) {
      return;
    }
    const app = this.app;
    const updateManager = this.updateManager;
    const settings = this.settings;
    const fetcher = new LinkerMetaInfoFetcher(app, settings);
    const isDirectory = app.vault.getAbstractFileByPath(file.path) instanceof import_obsidian6.TFolder;
    if (!isDirectory) {
      const metaInfo = fetcher.getMetaInfo(file);
      const contextMenuHandler = (event) => {
        const targetElement = event.target;
        if (!targetElement || !(targetElement instanceof HTMLElement)) {
          return;
        }
        const isMultipleReferences = targetElement.classList.contains("multiple-files-references") || targetElement.closest(".multiple-files-references") !== null;
        if (isMultipleReferences) {
          const virtualLinkSpan = targetElement.closest(".virtual-link-span") || targetElement.closest(".virtual-link");
          if (virtualLinkSpan) {
            virtualLinkSpan.classList.add("virtual-link-hover-lock");
            window.setTimeout(() => {
              virtualLinkSpan.classList.remove("virtual-link-hover-lock");
            }, 3e3);
          }
        }
        const virtualLinkElement = targetElement.closest(".virtual-link-a");
        const isVirtualLink = virtualLinkElement !== null;
        const isInTableCell = targetElement.closest("td, th") !== null;
        const linkElement = virtualLinkElement || targetElement;
        const from = parseInt(linkElement.getAttribute("from") || "-1");
        const to = parseInt(linkElement.getAttribute("to") || "-1");
        if (from === -1 || to === -1) {
          menu.addItem((item) => {
            item.setTitle("Converting link is not here").setIcon("link");
          });
        } else if (isVirtualLink) {
          menu.addItem((item) => {
            item.setTitle("Add to excluded keywords").setIcon("ban").onClick(async () => {
              const text = linkElement.getAttribute("origin-text") || "";
              if (text) {
                const newExcludedKeywords = [.../* @__PURE__ */ new Set([...settings.excludedKeywords, text])];
                await this.updateSettings({ excludedKeywords: newExcludedKeywords });
                updateManager.update();
              }
            });
          });
          if (isInTableCell) {
            menu.addItem((item) => {
              item.setTitle("Convert to real link (table mode)").setIcon("table").onClick(() => {
                handleTableCellConversion(linkElement, app, settings, updateManager);
              });
            });
          } else {
            menu.addItem((item) => {
              item.setTitle("Convert to real link").setIcon("link").onClick(() => {
                var _a2, _b2;
                const from2 = parseInt(linkElement.getAttribute("from") || "-1");
                const to2 = parseInt(linkElement.getAttribute("to") || "-1");
                if (from2 === -1 || to2 === -1) {
                  return;
                }
                const text = linkElement.getAttribute("origin-text") || "";
                const target = file;
                const activeFile = app.workspace.getActiveFile();
                const activeFilePath = (_a2 = activeFile == null ? void 0 : activeFile.path) != null ? _a2 : "";
                if (!activeFile) {
                  return;
                }
                if (!(target instanceof import_obsidian6.TFile)) {
                  return;
                }
                let absolutePath = target.path;
                let relativePath = relative(dirname(activeFile.path), dirname(absolutePath)) + "/" + basename(absolutePath);
                relativePath = relativePath.replace(/\\/g, "/");
                const replacementPath = app.metadataCache.fileToLinktext(target, activeFilePath);
                const headerId = linkElement.getAttribute("data-heading-id");
                const lastPart = replacementPath.split("/").pop();
                const shortestFile = app.metadataCache.getFirstLinkpathDest(lastPart || "", "");
                let shortestPath = (shortestFile == null ? void 0 : shortestFile.path) == target.path ? lastPart : absolutePath;
                const pathSuffix = headerId ? `#${headerId}` : "";
                if (!replacementPath.endsWith(".md")) {
                  if (absolutePath.endsWith(".md")) {
                    absolutePath = absolutePath.slice(0, -3);
                  }
                  if (shortestPath && shortestPath.endsWith(".md")) {
                    shortestPath = shortestPath.slice(0, -3);
                  }
                  if (relativePath.endsWith(".md")) {
                    relativePath = relativePath.slice(0, -3);
                  }
                  absolutePath += pathSuffix;
                  shortestPath += pathSuffix;
                  relativePath += pathSuffix;
                }
                const useMarkdownLinks = settings.useDefaultLinkStyleForConversion ? settings.defaultUseMarkdownLinks : settings.useMarkdownLinks;
                const linkFormat = settings.useDefaultLinkStyleForConversion ? settings.defaultLinkFormat : settings.linkFormat;
                const createLink = (replacementPath2, text2, markdownStyle) => {
                  if (markdownStyle) {
                    return `[${text2}](${replacementPath2})`;
                  } else {
                    const tableCellElement = targetElement.closest("td, th");
                    if (tableCellElement) {
                      const escapedText = text2.replace(/[\\|]/g, "\\$&");
                      return `[[${replacementPath2}\\|${escapedText}]]`;
                    } else {
                      return `[[${replacementPath2}|${text2}]]`;
                    }
                  }
                };
                let replacement = "";
                if (replacementPath === text && linkFormat === "shortest") {
                  replacement = `[[${replacementPath}]]`;
                } else {
                  if (linkFormat === "shortest") {
                    replacement = createLink(shortestPath || absolutePath, text, useMarkdownLinks);
                  } else if (linkFormat === "relative") {
                    replacement = createLink(relativePath, text, useMarkdownLinks);
                  } else if (linkFormat === "absolute") {
                    replacement = createLink(absolutePath, text, useMarkdownLinks);
                  }
                }
                const editor = (_b2 = app.workspace.getActiveViewOfType(import_obsidian6.MarkdownView)) == null ? void 0 : _b2.editor;
                const fromEditorPos = editor == null ? void 0 : editor.offsetToPos(from2);
                const toEditorPos = editor == null ? void 0 : editor.offsetToPos(to2);
                if (!fromEditorPos || !toEditorPos) {
                  return;
                }
                editor == null ? void 0 : editor.replaceRange(replacement, fromEditorPos, toEditorPos);
              });
            });
          }
        }
        activeDocument.removeEventListener("contextmenu", contextMenuHandler);
      };
      if (!metaInfo.excludeFile && (metaInfo.includeAllFiles || metaInfo.includeFile || metaInfo.isInIncludedDir)) {
        menu.addItem((item) => {
          item.setTitle("Exclude this file").setIcon("trash").onClick(async () => {
            var _a2;
            const target = file;
            const targetFile = app.vault.getFileByPath(target.path);
            if (!targetFile) {
              return;
            }
            const fileCache = app.metadataCache.getFileCache(targetFile);
            const frontmatter = (_a2 = fileCache == null ? void 0 : fileCache.frontmatter) != null ? _a2 : {};
            const tag = settings.tagToExcludeFile;
            let tags = frontmatter["tags"];
            if (typeof tags === "string") {
              tags = [tags];
            }
            if (!Array.isArray(tags)) {
              tags = [];
            }
            if (!tags.includes(tag)) {
              await app.fileManager.processFrontMatter(targetFile, (frontMatter) => {
                if (!frontMatter.tags) {
                  frontMatter.tags = /* @__PURE__ */ new Set();
                }
                const currentTags = [...frontMatter.tags];
                frontMatter.tags = /* @__PURE__ */ new Set([...currentTags, tag]);
                const includeTag = settings.tagToIncludeFile;
                if (frontMatter.tags instanceof Set && frontMatter.tags.has(includeTag)) {
                  frontMatter.tags.delete(includeTag);
                }
              }).catch(() => {
              });
              updateManager.update();
            }
          });
        });
      } else if (!metaInfo.includeFile && (!metaInfo.includeAllFiles || metaInfo.excludeFile || metaInfo.isInExcludedDir)) {
        menu.addItem((item) => {
          item.setTitle("Include this file").setIcon("plus").onClick(async () => {
            var _a2;
            const target = file;
            const targetFile = app.vault.getFileByPath(target.path);
            if (!targetFile) {
              return;
            }
            const fileCache = app.metadataCache.getFileCache(targetFile);
            const frontmatter = (_a2 = fileCache == null ? void 0 : fileCache.frontmatter) != null ? _a2 : {};
            const tag = settings.tagToIncludeFile;
            let tags = frontmatter["tags"];
            if (typeof tags === "string") {
              tags = [tags];
            }
            if (!Array.isArray(tags)) {
              tags = [];
            }
            if (!tags.includes(tag)) {
              await app.fileManager.processFrontMatter(targetFile, (frontMatter) => {
                if (!frontMatter.tags) {
                  frontMatter.tags = /* @__PURE__ */ new Set();
                }
                const currentTags = [...frontMatter.tags];
                frontMatter.tags = /* @__PURE__ */ new Set([...currentTags, tag]);
                const excludeTag = settings.tagToExcludeFile;
                if (frontMatter.tags instanceof Set && frontMatter.tags.has(excludeTag)) {
                  frontMatter.tags.delete(excludeTag);
                }
              }).catch(() => {
              });
              updateManager.update();
            }
          });
        });
      }
      activeDocument.addEventListener("contextmenu", contextMenuHandler, { once: true });
    } else {
      const path = file.path + "/";
      const isInIncludedDir = fetcher.includeDirPattern.test(path);
      const isInExcludedDir = fetcher.excludeDirPattern.test(path);
      if (fetcher.includeAllFiles && !isInExcludedDir || isInIncludedDir) {
        menu.addItem((item) => {
          item.setTitle("Exclude this directory").setIcon("trash").onClick(async () => {
            const target = file;
            const targetFolder = app.vault.getAbstractFileByPath(target.path);
            if (!targetFolder || !(targetFolder instanceof import_obsidian6.TFolder)) {
              return;
            }
            const newExcludedDirs = Array.from(/* @__PURE__ */ new Set([...settings.excludedDirectories, targetFolder.name]));
            const newIncludedDirs = settings.linkerDirectories.filter((dir) => dir !== targetFolder.name);
            await this.updateSettings({ linkerDirectories: newIncludedDirs, excludedDirectories: newExcludedDirs }).catch(() => {
            });
            updateManager.update();
          });
        });
      } else if (!fetcher.includeAllFiles && !isInIncludedDir || isInExcludedDir) {
        menu.addItem((item) => {
          item.setTitle("Include this directory").setIcon("plus").onClick(async () => {
            const target = file;
            const targetFolder = app.vault.getAbstractFileByPath(target.path);
            if (!targetFolder || !(targetFolder instanceof import_obsidian6.TFolder)) {
              return;
            }
            const newExcludedDirs = settings.excludedDirectories.filter((dir) => dir !== targetFolder.name);
            const newIncludedDirs = Array.from(/* @__PURE__ */ new Set([...settings.linkerDirectories, targetFolder.name]));
            await this.updateSettings({ linkerDirectories: newIncludedDirs, excludedDirectories: newExcludedDirs }).catch(() => {
            });
            updateManager.update();
          });
        });
      }
    }
  }
  cleanupVirtualLinks() {
    const virtualLinks = activeDocument.querySelectorAll(".virtual-link, .virtual-link-span, .virtual-link-a");
    virtualLinks.forEach((link) => {
      const anchor = link.classList.contains("virtual-link-a") ? link : link.querySelector(".virtual-link-a");
      const originalText = (anchor == null ? void 0 : anchor.getAttribute("origin-text")) || (anchor == null ? void 0 : anchor.textContent) || "";
      if (originalText) {
        const textNode = activeDocument.createTextNode(originalText);
        link.replaceWith(textNode);
      } else {
        link.remove();
      }
    });
    const multipleRefs = activeDocument.querySelectorAll(".multiple-files-references, .multiple-files-indicator");
    multipleRefs.forEach((ref) => ref.remove());
  }
  onunload() {
    this.cleanupVirtualLinks();
  }
  async loadSettings() {
    var _a2, _b2;
    this.settings = Object.assign({}, DEFAULT_SETTINGS, await this.loadData());
    try {
      const fileContent = await this.app.vault.adapter.read(this.app.vault.configDir + "/app.json");
      const appSettings = JSON.parse(fileContent);
      this.settings.defaultUseMarkdownLinks = (_a2 = appSettings.useMarkdownLinks) != null ? _a2 : false;
      this.settings.defaultLinkFormat = (_b2 = appSettings.newLinkFormat) != null ? _b2 : "shortest";
    } catch (e) {
      this.settings.defaultUseMarkdownLinks = false;
      this.settings.defaultLinkFormat = "shortest";
    }
  }
  async updateSettings(settings = {}) {
    Object.assign(this.settings, settings);
    const settingsToSave = { ...this.settings };
    delete settingsToSave.app;
    try {
      await this.saveData(settingsToSave);
    } catch (e) {
    }
    this.updateManager.update();
    if (!this.settings.linkerActivated) {
      this.cleanupVirtualLinks();
    }
    this.app.workspace.getLeavesOfType("markdown").forEach((leaf) => {
      const view = leaf.view;
      if (view instanceof import_obsidian6.MarkdownView && view.previewMode) {
        view.previewMode.rerender(true);
      }
    });
  }
};
var LinkerSettingTab = class extends import_obsidian6.PluginSettingTab {
  constructor(app, plugin) {
    super(app, plugin);
    this.plugin = plugin;
  }
  display() {
    const { containerEl } = this;
    containerEl.empty();
    new import_obsidian6.Setting(containerEl).setName(t("Auto-toggle activation status by mode")).setDesc(t("When enabled, the plugin will automatically activate in edit mode if inactive, and automatically deactivate in read mode if active")).addToggle((toggle) => toggle.setValue(this.plugin.settings.autoToggleByMode).onChange(async (value) => {
      await this.plugin.updateSettings({ autoToggleByMode: value });
      void this.plugin.handleLayoutChange();
    }));
    const quickAddCode = `module.exports = async (params) => {
    const id = 'fakelink';
    const pm = app.plugins;

    try {
        if (pm.plugins[id]) {
            await pm.disablePlugin(id);
            new Notice('Fake Link: OFF');
        } else {
            await pm.enablePlugin(id);
            new Notice('Fake Link: ON');
        }

        // Force refresh views first, then reload plugins
        const types = ['markdown', 'canvas'];
        const leaves = types.flatMap(t => app.workspace.getLeavesOfType(t));
        for (const leaf of leaves) {
            try {
                const s = leaf.getViewState();
                await leaf.setViewState({ ...s, state: { ...s.state, forceRefresh: true } });
            } catch (_) {}
        }
        app.workspace.trigger('layout-change');
        app.workspace.activeLeaf?.rebuildView();

        app.commands.executeCommandById('app:reload-plugins');
    } catch (e) {
        new Notice('Fake Link: toggle failed, check console');
    }
};`;
    new import_obsidian6.Setting(containerEl).setName(t("Activate virtual linker")).setDesc(t("Due to table and canvas rendering issues, to fully enable/disable virtual link rendering, use Quick Add or a third-party plugin to toggle the Fake Link plugin on/off.")).addToggle((toggle) => toggle.setValue(this.plugin.settings.linkerActivated).onChange(async (value) => {
      await this.plugin.updateSettings({ linkerActivated: value });
    })).addExtraButton((button) => button.setTooltip(t("Copy Quick Add script")).setIcon("clipboard-copy").onClick(async () => {
      await navigator.clipboard.writeText(quickAddCode);
      new import_obsidian6.Notice(t("Quick Add script copied to clipboard!"));
    }));
    new import_obsidian6.Setting(containerEl).setName(t("Matching behavior")).setHeading();
    new import_obsidian6.Setting(containerEl).setName(t("Include aliases")).setDesc(t("If enabled, the virtual linker will also match file aliases.")).addToggle((toggle) => toggle.setValue(this.plugin.settings.includeAliases).onChange(async (value) => {
      await this.plugin.updateSettings({ includeAliases: value });
    }));
    if (this.plugin.settings.advancedSettings) {
      new import_obsidian6.Setting(containerEl).setName(t("Only link once")).setDesc(t("When enabled, identical terms in the same note will only be linked once.")).addToggle((toggle) => toggle.setValue(this.plugin.settings.onlyLinkOnce).onChange(async (value) => {
        await this.plugin.updateSettings({ onlyLinkOnce: value });
      }));
      new import_obsidian6.Setting(containerEl).setName(t("Exclude links to real linked files")).setDesc(t("When enabled, terms that are already manually linked in the note will not be auto-linked.")).addToggle((toggle) => toggle.setValue(this.plugin.settings.excludeLinksToRealLinkedFiles).onChange(async (value) => {
        await this.plugin.updateSettings({ excludeLinksToRealLinkedFiles: value });
      }));
    }
    new import_obsidian6.Setting(containerEl).setName(t("Include headers")).setDesc(t("When enabled, Markdown headings (lines starting with #) will also be included for virtual linking.")).addToggle((toggle) => toggle.setValue(this.plugin.settings.includeHeaders).onChange(async (value) => {
      await this.plugin.updateSettings({ includeHeaders: value });
    }));
    new import_obsidian6.Setting(containerEl).setName(t("Enable header symbol keywords")).setDesc(t("When enabled, text between start and end symbols in headers will be used as virtual link keywords. Tip: use EasyTyping to select text and add symbols.")).addToggle((toggle) => toggle.setValue(this.plugin.settings.headerMatchSymbols).onChange(async (value) => {
      await this.plugin.updateSettings({ headerMatchSymbols: value });
    })).addExtraButton((button) => button.setTooltip(t("Copy EasyTyping template")).setIcon("clipboard-copy").onClick(async () => {
      await navigator.clipboard.writeText("\u27E6${0:${SEL}}\u27E7");
      new import_obsidian6.Notice(t("EasyTyping template copied to clipboard!"));
    }));
    if (this.plugin.settings.headerMatchSymbols) {
      new import_obsidian6.Setting(containerEl).setName(t("Start symbol")).setDesc(t("Symbol marking the start of the keyword in headers. Must be different from end symbol.")).addText((text) => text.setValue(this.plugin.settings.headerMatchStartSymbol).onChange(async (value) => {
        await this.plugin.updateSettings({ headerMatchStartSymbol: value });
      }));
      new import_obsidian6.Setting(containerEl).setName(t("End symbol")).setDesc(t("Symbol marking the end of the keyword in headers. Must be different from start symbol.")).addText((text) => text.setValue(this.plugin.settings.headerMatchEndSymbol).onChange(async (value) => {
        await this.plugin.updateSettings({ headerMatchEndSymbol: value });
      }));
      new import_obsidian6.Setting(containerEl).setName(t("Only match headers between symbols")).setDesc(t("When enabled, only headers containing start and end symbols will produce virtual links. Unmarked headers will not produce virtual links.")).addToggle((toggle) => toggle.setValue(this.plugin.settings.headerMatchOnlyBetweenSymbols).onChange(async (value) => {
        await this.plugin.updateSettings({ headerMatchOnlyBetweenSymbols: value });
      }));
    }
    new import_obsidian6.Setting(containerEl).setName(t("Allow virtual links in headers")).setDesc(t("When enabled, virtual links will be displayed inside Markdown headings. Tip: use with Quick Switcher++ for header navigation.")).addToggle((toggle) => toggle.setValue(this.plugin.settings.allowLinksInHeaders).onChange(async (value) => {
      await this.plugin.updateSettings({ allowLinksInHeaders: value });
    }));
    new import_obsidian6.Setting(containerEl).setName(t("Match any part of a word")).setDesc(t("When disabled, only complete word matches are linked. When enabled, any substring match will be linked.")).addToggle((toggle) => toggle.setValue(this.plugin.settings.matchAnyPartsOfWords).onChange(async (value) => {
      await this.plugin.updateSettings({ matchAnyPartsOfWords: value });
    }));
    if (!this.plugin.settings.matchAnyPartsOfWords) {
      new import_obsidian6.Setting(containerEl).setName(t("Match the beginning of words")).setDesc(t("When enabled, word prefixes will be linked even without complete word matches.")).addToggle((toggle) => toggle.setValue(this.plugin.settings.matchBeginningOfWords).onChange(async (value) => {
        await this.plugin.updateSettings({ matchBeginningOfWords: value });
      }));
      new import_obsidian6.Setting(containerEl).setName(t("Match the end of words")).setDesc(t("When enabled, word suffixes will be linked even without complete word matches.")).addToggle((toggle) => toggle.setValue(this.plugin.settings.matchEndOfWords).onChange(async (value) => {
        await this.plugin.updateSettings({ matchEndOfWords: value });
      }));
    }
    if (this.plugin.settings.matchAnyPartsOfWords || this.plugin.settings.matchBeginningOfWords) {
      new import_obsidian6.Setting(containerEl).setName(t("Suppress suffix for sub words")).setDesc(t("When enabled, the link suffix will only be shown for complete word matches, not partial matches.")).addToggle((toggle) => toggle.setValue(this.plugin.settings.suppressSuffixForSubWords).onChange(async (value) => {
        await this.plugin.updateSettings({ suppressSuffixForSubWords: value });
      }));
    }
    if (this.plugin.settings.advancedSettings) {
      new import_obsidian6.Setting(containerEl).setName(t("Fix ime typing issues")).setDesc(t("This option is recommended when using ime for typing non-latin scripts such as chinese, japanese, or korean and prevents virtual linking from interfering with ime composition at the start of lines.")).addToggle((toggle) => toggle.setValue(this.plugin.settings.fixIMEProblem).onChange(async (value) => {
        await this.plugin.updateSettings({ fixIMEProblem: value });
      }));
    }
    if (this.plugin.settings.advancedSettings) {
      new import_obsidian6.Setting(containerEl).setName(t("Avoid linking in current line")).setDesc(t("If activated, there will be no links in the current line.")).addToggle((toggle) => toggle.setValue(this.plugin.settings.excludeLinksInCurrentLine).onChange(async (value) => {
        await this.plugin.updateSettings({ excludeLinksInCurrentLine: value });
      }));
    }
    new import_obsidian6.Setting(containerEl).setName(t("Maximum references to show")).setDesc(t("The maximum number of reference markers [1][2]... shown after a virtual link.")).addText((text) => {
      text.setValue(String(this.plugin.settings.maxReferenceCount)).onChange(async (value) => {
        const n = parseInt(value);
        if (n > 0)
          await this.plugin.updateSettings({ maxReferenceCount: n });
      });
      text.inputEl.type = "number";
      text.inputEl.min = "1";
      text.inputEl.max = "20";
    });
    new import_obsidian6.Setting(containerEl).setName(t("Hide link when references exceed")).setDesc(t("When the total number of matching files (names + aliases + headers) exceeds this threshold, the virtual link will not be displayed.")).addText((text) => {
      text.setValue(String(this.plugin.settings.maxReferencesToHideLink)).onChange(async (value) => {
        const n = parseInt(value);
        if (n > 0)
          await this.plugin.updateSettings({ maxReferencesToHideLink: n });
      });
      text.inputEl.type = "number";
      text.inputEl.min = "1";
      text.inputEl.max = "50";
    });
    new import_obsidian6.Setting(containerEl).setName(t("Case sensitivity")).setHeading();
    new import_obsidian6.Setting(containerEl).setName(t("Case sensitive")).setDesc(t("If activated, the matching is case sensitive.")).addToggle((toggle) => toggle.setValue(this.plugin.settings.matchCaseSensitive).onChange(async (value) => {
      await this.plugin.updateSettings({ matchCaseSensitive: value });
    }));
    if (this.plugin.settings.advancedSettings) {
      new import_obsidian6.Setting(containerEl).setName(t("Capital letter percentage for automatic match case")).setDesc(t("The percentage (0 - 100) of capital letters in a file name or alias to be automatically considered as case sensitive.")).addText((text) => text.setValue((this.plugin.settings.capitalLetterProportionForAutomaticMatchCase * 100).toFixed(1)).onChange(async (value) => {
        let newValue = parseFloat(value);
        if (isNaN(newValue)) {
          newValue = 75;
        } else if (newValue < 0) {
          newValue = 0;
        } else if (newValue > 100) {
          newValue = 100;
        }
        newValue /= 100;
        await this.plugin.updateSettings({ capitalLetterProportionForAutomaticMatchCase: newValue });
      }));
      if (this.plugin.settings.matchCaseSensitive) {
        new import_obsidian6.Setting(containerEl).setName(t("Tag to ignore case")).setDesc(t("By adding this tag to a file, the linker will ignore the case for the file.")).addText((text) => text.setValue(this.plugin.settings.tagToIgnoreCase).onChange(async (value) => {
          await this.plugin.updateSettings({ tagToIgnoreCase: value });
        }));
      } else {
        new import_obsidian6.Setting(containerEl).setName(t("Tag to match case")).setDesc(t("By adding this tag to a file, the linker will match the case for the file.")).addText((text) => text.setValue(this.plugin.settings.tagToMatchCase).onChange(async (value) => {
          await this.plugin.updateSettings({ tagToMatchCase: value });
        }));
      }
      new import_obsidian6.Setting(containerEl).setName(t("Property name to ignore case")).setDesc(t("By adding this property to a note, containing a list of names, the linker will ignore the case for the specified names / aliases. This way you can decide, which alias should be insensitive.")).addText((text) => text.setValue(this.plugin.settings.propertyNameToIgnoreCase).onChange(async (value) => {
        await this.plugin.updateSettings({ propertyNameToIgnoreCase: value });
      }));
      new import_obsidian6.Setting(containerEl).setName(t("Property name to match case")).setDesc(t("By adding this property to a note, containing a list of names, the linker will match the case for the specified names / aliases. This way you can decide, which alias should be case sensitive.")).addText((text) => text.setValue(this.plugin.settings.propertyNameToMatchCase).onChange(async (value) => {
        await this.plugin.updateSettings({ propertyNameToMatchCase: value });
      }));
    }
    new import_obsidian6.Setting(containerEl).setName(t("Matched files")).setHeading();
    new import_obsidian6.Setting(containerEl).setName(t("Include all files")).setDesc(t("Include all files for the virtual linker.")).addToggle((toggle) => toggle.setValue(this.plugin.settings.includeAllFiles).onChange(async (value) => {
      await this.plugin.updateSettings({ includeAllFiles: value });
    }));
    if (!this.plugin.settings.includeAllFiles) {
      new import_obsidian6.Setting(containerEl).setName(t("Glossary linker directories")).setDesc(t("Directories to include for the virtual linker (separated by new lines).")).addTextArea((text) => {
        let setValue = "";
        try {
          setValue = this.plugin.settings.linkerDirectories.join("\n");
        } catch (e) {
        }
        text.setPlaceholder("List of directory names (separated by new line)").setValue(setValue).onChange(async (value) => {
          this.plugin.settings.linkerDirectories = value.split("\n").map((x) => x.trim()).filter((x) => x.length > 0);
          await this.plugin.updateSettings();
        });
        text.inputEl.addClass("linker-settings-text-box");
      });
    } else {
      if (this.plugin.settings.advancedSettings) {
        new import_obsidian6.Setting(containerEl).setName(t("Excluded directories")).setDesc(t("Directories from which files are to be excluded for the virtual linker (separated by new lines). Files in these directories will not create any virtual links in other files.")).addTextArea((text) => {
          let setValue = "";
          try {
            setValue = this.plugin.settings.excludedDirectories.join("\n");
          } catch (e) {
          }
          text.setPlaceholder("List of directory names (separated by new line)").setValue(setValue).onChange(async (value) => {
            this.plugin.settings.excludedDirectories = value.split("\n").map((x) => x.trim()).filter((x) => x.length > 0);
            await this.plugin.updateSettings();
          });
          text.inputEl.addClass("linker-settings-text-box");
        });
      }
    }
    if (this.plugin.settings.advancedSettings) {
      new import_obsidian6.Setting(containerEl).setName(t("Tag to include file")).setDesc(t("Tag to explicitly include the file for the linker.")).addText((text) => text.setValue(this.plugin.settings.tagToIncludeFile).onChange(async (value) => {
        await this.plugin.updateSettings({ tagToIncludeFile: value });
      }));
      new import_obsidian6.Setting(containerEl).setName(t("Tag to ignore file")).setDesc(t("Tag to ignore the file for the linker.")).addText((text) => text.setValue(this.plugin.settings.tagToExcludeFile).onChange(async (value) => {
        await this.plugin.updateSettings({ tagToExcludeFile: value });
      }));
      new import_obsidian6.Setting(containerEl).setName(t("Exclude self-links to the current note")).setDesc(t("If toggled, links to the note itself are excluded from the linker, but this might not work in reading view.")).addToggle((toggle) => toggle.setValue(this.plugin.settings.excludeLinksToOwnNote).onChange(async (value) => {
        await this.plugin.updateSettings({ excludeLinksToOwnNote: value });
      }));
      new import_obsidian6.Setting(containerEl).setName(t("Excluded directories for generating virtual links")).setDesc(t("Directories in which the plugin will not create virtual links (separated by new lines).")).addTextArea((text) => {
        let setValue = "";
        try {
          setValue = this.plugin.settings.excludedDirectoriesForLinking.join("\n");
        } catch (e) {
        }
        text.setPlaceholder("List of directory names (separated by new line)").setValue(setValue).onChange(async (value) => {
          this.plugin.settings.excludedDirectoriesForLinking = value.split("\n").map((x) => x.trim()).filter((x) => x.length > 0);
          await this.plugin.updateSettings();
        });
        text.inputEl.addClass("linker-settings-text-box");
      });
      new import_obsidian6.Setting(containerEl).setName(t("Excluded keywords")).setDesc(t("Keywords to exclude from virtual linking (comma separated). Files/aliases or headings matching these keywords will not be linked.")).addTextArea((text) => {
        text.setValue(this.plugin.settings.excludedKeywords.join(",")).onChange(async (value) => {
          const keywords = value.split(",").map((x) => x.trim()).filter((x) => x.length > 0);
          await this.plugin.updateSettings({ excludedKeywords: keywords });
        });
        text.inputEl.addClass("linker-settings-text-box");
      });
      new import_obsidian6.Setting(containerEl).setName(t("Excluded file extensions")).setDesc(t("File extensions to exclude from virtual linking (one per line or comma separated)")).addTextArea((text) => {
        text.setValue(this.plugin.settings.excludedExtensions.join("\n")).onChange(async (value) => {
          const extensions = value.split(/[\n,]/).map((x) => x.trim()).filter((x) => x.length > 0).map((x) => x.startsWith(".") ? x : `.${x}`);
          await this.plugin.updateSettings({ excludedExtensions: extensions });
        });
        text.inputEl.addClass("linker-settings-text-box");
      });
    }
    new import_obsidian6.Setting(containerEl).setName(t("Auto-insert symbol into headers")).setDesc(t("When enabled, a unique symbol is automatically placed at the front of new or modified header text, preventing accidental matching by regular body text.")).addToggle((toggle) => toggle.setValue(this.plugin.settings.headerAutoAppendSuffix).onChange(async (value) => {
      await this.plugin.updateSettings({ headerAutoAppendSuffix: value });
    }));
    if (this.plugin.settings.headerAutoAppendSuffix) {
      new import_obsidian6.Setting(containerEl).setName(t("Header marker symbol")).setDesc(t("The symbol placed at the front of header text (after # but before content). Use a rare character not found in normal text.")).addText((text) => text.setValue(this.plugin.settings.headerAutoAppendSymbol).onChange(async (value) => {
        await this.plugin.updateSettings({ headerAutoAppendSymbol: value });
      }));
    }
    new import_obsidian6.Setting(containerEl).setName(t("Link style")).setHeading();
    new import_obsidian6.Setting(containerEl).setName(t("Color-only display")).setDesc(t("When enabled, virtual links are shown in a custom text color instead of the default background shadow.")).addToggle((toggle) => toggle.setValue(this.plugin.settings.colorOnlyDisplay).onChange(async (value) => {
      await this.plugin.updateSettings({ colorOnlyDisplay: value });
      const doc = this.containerEl.ownerDocument;
      if (value) {
        doc.body.classList.add("virtual-link-color-only");
        doc.body.style.setProperty("--virtual-link-color", this.plugin.settings.virtualLinkColor);
      } else {
        doc.body.classList.remove("virtual-link-color-only");
        doc.body.style.removeProperty("--virtual-link-color");
      }
    }));
    new import_obsidian6.Setting(containerEl).setName(t("Virtual link color")).setDesc(t("Custom CSS color for virtual links (e.g., #409eff, var(--text-accent)). Only used when color-only display is enabled. (Restart plugin to apply)")).addText((text) => {
      text.setValue(this.plugin.settings.virtualLinkColor).onChange(async (value) => {
        await this.plugin.updateSettings({ virtualLinkColor: value });
        if (this.plugin.settings.colorOnlyDisplay) {
          this.containerEl.ownerDocument.body.style.setProperty("--virtual-link-color", value);
        }
      });
      text.inputEl.placeholder = "#409eff";
    });
    new import_obsidian6.Setting(containerEl).setName(t("Alternative display style")).setDesc(t("When enabled, strikethrough is replaced with underline, and %%comments%% are collapsed into small dots that expand on the active line.")).addToggle((toggle) => toggle.setValue(this.plugin.settings.alternativeDisplayStyle).onChange(async (value) => {
      await this.plugin.updateSettings({ alternativeDisplayStyle: value });
      const doc = this.containerEl.ownerDocument;
      if (value) {
        doc.body.classList.add("virtual-linker-alt-style");
      } else {
        doc.body.classList.remove("virtual-linker-alt-style");
      }
    }));
    new import_obsidian6.Setting(containerEl).setName(t("Always show multiple references")).setDesc(t("If toggled, if there are multiple matching notes, all references are shown behind the match. If not toggled, the references are only shown if hovering over the match.")).addToggle((toggle) => toggle.setValue(this.plugin.settings.alwaysShowMultipleReferences).onChange(async (value) => {
      await this.plugin.updateSettings({ alwaysShowMultipleReferences: value });
    }));
    new import_obsidian6.Setting(containerEl).setName(t("Virtual link suffix")).setDesc(t("The suffix to add to auto generated virtual links.")).addText((text) => text.setValue(this.plugin.settings.virtualLinkSuffix).onChange(async (value) => {
      await this.plugin.updateSettings({ virtualLinkSuffix: value });
    }));
    new import_obsidian6.Setting(containerEl).setName(t("Virtual link suffix for aliases")).setDesc(t("The suffix to add to auto generated virtual links for aliases.")).addText((text) => text.setValue(this.plugin.settings.virtualLinkAliasSuffix).onChange(async (value) => {
      await this.plugin.updateSettings({ virtualLinkAliasSuffix: value });
    }));
    new import_obsidian6.Setting(containerEl).setName(t("Apply default link styling")).setDesc(t("If toggled, the default link styling will be applied to virtual links. Furthermore, you can style the links yourself with a CSS-snippet affecting the class `virtual-link`. (Find the CSS snippet directory at Appearance -> CSS Snippets -> Open snippets folder)")).addToggle((toggle) => toggle.setValue(this.plugin.settings.applyDefaultLinkStyling).onChange(async (value) => {
      await this.plugin.updateSettings({ applyDefaultLinkStyling: value });
    }));
    new import_obsidian6.Setting(containerEl).setName(t("Use default link style for conversion")).setDesc(t("If toggled, the default link style will be used for the conversion of virtual links to real links.")).addToggle((toggle) => toggle.setValue(this.plugin.settings.useDefaultLinkStyleForConversion).onChange(async (value) => {
      await this.plugin.updateSettings({ useDefaultLinkStyleForConversion: value });
    }));
    if (!this.plugin.settings.useDefaultLinkStyleForConversion) {
      new import_obsidian6.Setting(containerEl).setName(t("Use [[wikilinks]]")).setDesc(t("If toggled, the virtual links will be created as wikilinks instead of Markdown links.")).addToggle((toggle) => toggle.setValue(!this.plugin.settings.useMarkdownLinks).onChange(async (value) => {
        await this.plugin.updateSettings({ useMarkdownLinks: !value });
      }));
      new import_obsidian6.Setting(containerEl).setName(t("Link format")).setDesc(t("The format of the generated links.")).addDropdown((dropdown) => dropdown.addOption("shortest", "Shortest").addOption("relative", "Relative").addOption("absolute", "Absolute").setValue(this.plugin.settings.linkFormat).onChange(async (value) => {
        await this.plugin.updateSettings({ linkFormat: value });
      }));
    }
  }
};

/* nosourcemap */