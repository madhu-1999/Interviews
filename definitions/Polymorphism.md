---
tags:
  - "#oop"
---
It allows a single interface, method, or action to behave differently depending on the object.
+ [p] Enables code extensibility; new object types can be added without changing existing logic.
>[!example] A `Shape` class has a `draw()` method; a `Circle` draws a ring, while a `Square` draws four lines.

[Different Types of Polymorphism](https://krishna.github.io/posts/many-types-of-polymorphism/)