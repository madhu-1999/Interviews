A concrete instance of a class that exists physically in memory. It possesses a distinct state (assigned values), specific behaviors (callable methods), and a unique identity (memory address).
>[!Example] An **Object** is the actual cookie made from a cookie cutter i.e. class, each capable of having different toppings (states).
